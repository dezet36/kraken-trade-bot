# /etc/needrestart/conf.d/kraken.conf
# Бот и модель после обновления пакетов не перезапускаются: такой перезапуск
# обрывает разбор модели (25.09.2026 06:43 — переделка XLMUSDT). Их
# перезапускают только ops/restart_when_idle.sh и ops/llm_guard.sh.
$nrconf{override_rc}{qr(^kraken-)} = 0;
