# Зависимости Kraken trading bot (пины = версии, проверенные в работе 2026-07-05)
ccxt==4.5.59
pandas==3.0.3
numpy==2.5.0
requests==2.34.2
APScheduler==3.11.2
python-dotenv==1.2.2
cryptography==49.0.0
matplotlib==3.11.0
mplfinance==0.12.10b0

# Окно настольного приложения. Без него desktop.py открывал дашборд окном
# Chrome в режиме --app: внешне похоже, но это чужая программа, её может не
# оказаться на машине, и вести себя она будет по-своему. С pywebview окно
# своё, движок — WebView2, встроенный в Windows 10 и новее.
pywebview==6.2.1

# Polymarket: подпись и отправка заявок в CLOB. Нужен только для живого
# режима; бумажный расчёт работает и без него.
# Клиент второго поколения. Старый py-clob-client ЗААРХИВИРОВАН самими
# разработчиками площадки, и биржа отвечает ему «invalid order version, please
# use the latest clob-client» — выяснено отправкой настоящей заявки.
# Старый оставлен: часть кода ещё читает им стаканы, и убирать его надо
# отдельной правкой, а не заодно с переводом торгового пути.
py-clob-client>=0.34
py-clob-client-v2>=1.1.0

# ПОДПИСКА НА СТАКАН. Опрос не успевает: между двумя запросами рынок проходит
# сквозь нашу заявку, и подбирают нас ровно тогда, когда она перестала быть
# выгодной. Замерено — край стоящих котировок +0.0095, а к моменту исполнения
# остаётся +0.0005. Отдельная библиотека для веб-сокета не нужна: aiohttp уже
# приходит зависимостью и умеет всё, что требуется.
aiohttp>=3.9
