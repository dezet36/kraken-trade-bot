# Замечания по анализу ИИ — на проверку статистикой

Живой список того, что замечено в разборах модели, но не исправлено сразу:
сначала набираем цифры (см. `llm_features.jsonl`, `llm_outcomes.csv`,
`paper_trades.csv`, `llm_calls.csv`), потом решаем. Каждый пункт — что
замечено, где, чем измерить, при каком результате что делать.

Статусы: **наблюдаем** → **решено: правим / не правим** → **исправлено (коммит)**.

---

## 1. Стоп над незакрытым имбалансом старшего ТФ

- **Замечено:** 21.09.2026, SHIB1000USDT 16:41 и ADAUSDT 17:06.
- **Суть:** модель ставит стоп сразу над незакрытым имбалансом 4ч (SHIB —
  внутри имбаланса 0.005586–0.005655, стоп 0.005647; ADA — на 0.1% выше
  имбаланса 0.2293–0.2300, стоп 0.2303). Имбаланс старшего ТФ — магнит:
  откат обычно его заполняет и снимает такой стоп. Стоп за имбалансом
  стоил бы ~1% дистанции при R:R 3.5–4 вместо 5–5.7.
- **Измерить:** по `llm_outcomes.csv` — для планов, у которых стоп лежит
  внутри или в пределах 0.3% над имбалансом 4ч (по `llm_markup/` и уровням
  вида «низ/верх имбаланса 4ч» из `llm_calls.levels`): доля `hit_sl` = 1 при
  том, что цена позже дошла до цели (`hit_tp1` = 1 после `sl_hours`), против
  той же доли у планов со стопом за имбалансом.
- **Решение:** если у «стоп над имбалансом» снятых с последующим ходом к цели
  заметно больше (≥ 1.5×) — проверка кода «стоп над незакрытым имбалансом
  старшего ТФ» с отказом или требованием перенести стоп за него; и фраза в
  промт. Иначе — оставить, R:R важнее.
- **Измерено 22.09.2026** (`docs/Аудит_ИИ_2026-09-22.md`, раздел 2): по 16
  планам с сохранённой разметкой имбаланс 4ч лежит между входом и стопом у 9,
  стоп ВНУТРИ имбаланса — у 4 (SHIB 21.09 16:41, SOL 22.09 02:22,
  DOGE 22.09 10:34, XLM 22.09 12:35). Со стопом за дальним краем R:R падает
  3.05 → 1.66 (SOL), 2.87 → 1.49 (DOGE), 2.54 → 1.15 (XLM) — то есть тесный
  стоп добывает недостающее отношение. Найдена дыра в коде: ворота
  `stop_inside_entry_zone` читают только часовые зоны (`market['fvgs']`,
  `market['pois']`), а зоны 4ч (`market['htf_zones']`) не смотрит никто,
  хотя промт отправляет модель именно к ним.
- **Сделано 23.09.2026.** Общий слой считает уровень слома структуры
  (`smc.structure.invalidation_level`), разметка отдаёт его модели как
  обычный уровень («слом структуры 1ч/4ч»), промт требует стоп за ним, а
  код отвергает план тремя воротами: «план против структуры» (вход по
  мёртвую сторону или стоп, не доходящий до слома), «стоп внутри живой
  зоны» (теперь читает и зоны 4ч — та самая дыра), «цель недостижима».
- **Статус:** закрыто, наблюдаем воронку отказов.

## 2. Условие входа слишком строгое в тренде (ретест / вынос-возврат)

- **Замечено:** 21.09.2026 — SUI 07:07 (retest), LTC 12:59 (retest), SOL 10:05
  (sweep_reclaim): цена ушла к цели без входа; из 5 принятых планов входов 0.
- **Суть:** в сильном тренде модель просит откат в зону на 2–5% и
  подтверждение; за 17–25 минут разбора цена уходит. Правило «вход из
  зоны» верно, условие поверх него — лишнее, когда цена уже в зоне.
- **Измерить:** отказы `цель достигнута без входа` и `рынок обогнал план`
  (`refused`), колонки `missed_move_r`, `cond_hours`, `entry_touched`,
  `min_dist_entry_pct` в исходах. Доля планов с `missed_move_r ≥ 1` при
  `entry_touched` = 0.
- **Решение:** если таких ≥ 30% принятых планов — при сборе вердикта:
  цена в зоне входа или ≤ 1 ATR над ней и старший ТФ за план → лимит
  сразу; дальше 1 ATR — план с условием; старший ТФ против — только
  условие. Вход при этом никогда не смещается выше плана.
- **Измерено 22.09.2026:** цель достигнута раньше входа у 3 планов из 22
  (SUI 05:41, SUI 07:07, LTC 21.09 12:59); у LTC до цели было 0.92%, а до
  входа 4.13% — вчетверо дальше. Цель уже посещалась за сутки до разбора у
  5 планов из 19 (26%). Отдельного замера требует не «строгость условия», а
  соотношение плеч: план, где до цели ближе, чем до входа, обречён.
- **Статус:** наблюдаем; добавлено следствие — сравнение «до цели против до
  входа» как кандидат в ворота кода.

## 3. Сопровождение позиции: безубыток при +1R, часть на первой цели

- **Замечено:** 20.09.2026 — 4 из 8 стопов были в +1.2…+2.7R и вернулись
  к стопу (LINK, COTI, SUI, ARB).
- **Измерить:** `paper_trades.csv`: `first_1r_min`, `r_if_be_1r` против
  `pnl_r`; сумма `r_if_be_1r` − сумма `pnl_r` по всем закрытым сделкам ИИ.
- **Решение:** если безубыток при +1R даёт в сумме больше R, чем режет
  (стопы после +1R, которые потом дошли бы до цели) — включить для ИИ
  `be_level` при +1R и половину на TP1. Иначе — оставить как есть.
- **Измерено 22.09.2026:** по 11 закрытым сделкам ИИ безубыток при +1R даёт
  **−3.30R вместо −7.55R** (+4.25R). Четыре убыточные сделки были в плюсе на
  1.2–2.7R (COTI +2.71, LINK +2.24, ARB +1.32, SUI +1.21) и вернулись к
  стопу; частичной фиксации не было ни в одной (`tps_hit` = 0 везде, кроме
  единственной прибыльной). Первая цель при этом стояла в 1.97–9.72R
  (медиана ~3.6R) — дальше, чем рынок ходил.
- **Сделано 23.09.2026.** Безубыток при +1R включён (`be_level` = вход ± 1R),
  доли выхода — половина на первой цели.
- **Статус:** закрыто, считаем `r_if_be_1r` против `pnl_r` дальше.

## 4. Вход у нижнего края имбаланса 4ч (полное заполнение)

- **Замечено:** ADAUSDT 21.09 17:06 — вход 0.2345 при имбалансе 0.2345–0.2395.
- **Суть:** ожидание полного заполнения имбаланса; обычная практика — вход
  от середины (50%). Консервативно, и это одна из причин пропущенных входов.
- **Измерить:** по исходам — `min_dist_entry_pct` для планов, где вход =
  нижний край имбаланса (LONG) / верхний (SHORT): как часто цена не доходит
  0.5–1% до входа и уходит к цели.
- **Решение:** если часто — в промт: «вход из имбаланса — от середины, стоп
  за дальним краем». Связано с п. 2.
- **Статус:** наблюдаем.

## 5. Блоки разметки, которые модель не читает

- **Замечено:** обзор 17 разборов 21.09: «BTC как ориентир» — 1 из 17,
  «исходы прошлых планов» — 1 из 17, сессии — 7 из 17; ОИ — 8 из 17.
- **Измерить:** повторить подсчёт упоминаний по `llm_calls.csv` через неделю
  (regex по мысли + разбору), плюс — есть ли связь исходов с `btc_24h_pct`,
  `vs_btc_24h_pp` в признаках (если признак предсказывает исход, а модель его
  не читает — надо заставить читать; если не предсказывает — убрать блок).
- **Решение:** убрать неиспользуемые блоки (~150 токенов, −20 с на разбор)
  или ввести явный пункт чек-листа.
- **Статус:** наблюдаем.

## 6. Поле «встречный план» (alt) выходит мусором

- **Замечено:** SHIB1000USDT 21.09 16:41 — `alt` = «:{».
- **Измерить:** доля разборов с `alt` короче 20 знаков в `llm_calls.csv`.
- **Решение:** если > 5% — минимальная длина поля в грамматике (`_text`
  с нижней границей) или убрать поле из ответа, если оно не используется
  критиком/человеком.
- **Статус:** наблюдаем.

## 7. Скорость хоста VPS плавает

- **Замечено:** 21.09: чтение вопроса 9.9 ток/с ночью, 7.6–8.7 днём,
  4.7 в 14:35–15:08 (NEAR — 45 мин вместо 17). Своих потребителей CPU нет,
  свопа нет.
- **Измерить:** `steal` в `vmstat 5` во время разбора; `prompt_tokens/seconds`
  по признакам с разбивкой по часу UTC (`hour_utc`).
- **Решение:** если steal > 10% в определённые часы — вопрос к провайдеру /
  тариф с выделенными ядрами; если нет — искать у себя.
- **Статус:** наблюдаем.

## 8. Счётчик «N св. 4ч назад» у зон старшего ТФ

- **Замечено:** SHIB 21.09 — ордер-блок 4ч 0.005421–0.005526 подписан
  «1 св. 4ч назад», по свечам — 4.
- **Суть:** косметика, на решение не влияет; но модель оценивает свежесть зон.
- **Решение:** проверить расчёт `bars_ago` в `htf_zones` при случае.
- **Статус:** не срочно.

## 9. Критик выключен с 20.09 21:54 UTC

- **Суть:** планы идут без второго взгляда; время модели дороже, и код
  отклоняет 2/3 планов сам.
- **Измерить:** доля планов, принятых кодом и закрытых по стопу, у которых
  критик (по прошлым записям `critic` в журнале) отклонял похожие.
- **Решение:** включить обратно, когда время разбора упадёт ниже 15 минут
  или если стопов после принятия кодом окажется > 60%.
- **Статус:** наблюдаем.

## 10. Нестабильный тест в полном прогоне

- **Замечено:** 21.09 — в 2 из 6 полных прогонов падали 1–2 теста; на
  повторах и по отдельности проходят. Имя не поймано.
- **Решение:** при следующем падении — сохранить вывод (`pytest -q > файл`),
  найти общее состояние модуля между тестами.
- **Статус:** наблюдаем.

## 11. Вероятность p — константа, ворота EV мертвы

- **Замечено:** 22.09.2026 при аудите.
- **Суть:** модель ставит `p` = 0.65 в 41 плане из 68 (медиана 0.65). EV
  считается как `p·RR − (1−p) − издержки`, и при `p ≥ 0.55` с `R:R ≥ 2.5`
  (меньше не пропустят предыдущие ворота) EV всегда ≥ 0.7. Отказ «ожидание
  не положительно» не сработал ни разу за 179 разборов — он не может
  сработать по построению. Фактическая доля «цель раньше стопа» — 0 из 39
  завершённых наблюдений, 1 из 11 сделок.
- **Измерить:** доля планов, дошедших до цели раньше стопа, в разрезе
  геометрии (стоп в долях дневного размаха × цель в R). Это и есть p.
- **Решение:** либо считать `p` по своей статистике и оставить ворота, либо
  убрать EV-ворота и не показывать EV как число, которому можно верить.
- **Статус:** новое, из аудита.

## 12. Геометрия плана: стоп 0.33 дневного размаха, цель 3.41R

- **Замечено:** 22.09.2026, по 24 последним принятым планам.
- **Суть:** медиана стопа — 0.33 дневного размаха (18 планов из 24 теснее
  0.4), медиана первой цели — 3.41R (при этом в дневных размахах цель
  умеренная, ~1.2). Для блуждания без сноса вероятность дойти до цели
  раньше стопа ≈ 1/(1+3.41) ≈ 23%. Наблюдаемое: стоп задет у 29 из 39
  планов (74%), цель — у 4 (10%).
- **Измерить:** ту же пару чисел раз в неделю; отдельно — долю планов,
  где стоп теснее половины дневного размаха.
- **Решение:** не порог сам по себе, а связка: стоп за дальним краем зоны
  (п. 1) естественно расширит стоп и опустит R:R; тогда часть планов уйдёт
  в отказ, а оставшиеся будут с честной геометрией.
- **Статус:** новое, из аудита.

## 13. Часовой горизонт против издержек

- **Замечено:** 23.09.2026, при переводе ИИ на стоп за сломом структуры.
- **Суть:** решение владельца — часовой план. Со стопом за сломом структуры
  1ч дистанция выходит 1–4% (медиана по замеру 1.1%), и комиссии круга
  (0.075%) съедают до 7% риска — выше предела ИИ (5%). Минимальный стоп по
  издержкам 1.5% работает фильтром: майоры (BTC, ETH, SOL, SUI) с тесной
  часовой структурой в часовой план не попадают вовсе. Замер по 21 паре на
  23.09: проходят 5.
- **Измерить:** долю отказов «стоп теснее минимального» в воронке и число
  принятых планов в сутки. Если принятых меньше одного в сутки — часовой
  горизонт при этих издержках не живёт.
- **Решение:** если поток пересохнет — переводить ИИ на свинг 4ч (стоп за
  сломом 4ч ~7%, цель 12–14%, издержки 1% риска, удержание дни).
- **Статус:** новое, наблюдаем.

## 14. Выход по закрытию за уровнем слома — замерен, отклонён

- **Замечено:** 23.09.2026, при разборе схемы входа.
- **Идея была:** если часовая свеча ЗАКРЫЛАСЬ за уровнем слома структуры,
  идея мертва — закрывать позицию сразу, не дожидаясь стопа.
- **Возражение владельца:** следующей свечой цена может вернуться в
  структуру и пойти дальше; тогда мы ловим ложные сломы.
- **Измерено** на 304 сломах структуры (8 пар, ~2 месяца часовых свечей,
  вход у уровня, риск 1.75%, цель 2R):
  - цена закрывается обратно за уровень: за 1 свечу — 32%, за 2 — 42%,
    за 3 — **50%**. Возражение верное: половина сломов ложные;
  - но ожидание возврата не окупается:

    | политика | средний R |
    |---|---|
    | держать до стопа/цели (как сейчас) | −0.279 |
    | выйти по закрытию свечи слома | −0.225 |
    | дать ОДНУ свечу на возврат | −0.228 |
    | дать ДВЕ свечи на возврат | −0.298 |

  - цель 2R раньше стопа — всего 18% случаев.
- **Решение: не делать.** Выигрыш 0.05R на событие при ~7 событиях в месяц
  — треть R в месяц, меньше разброса между сетапами. Сложности не стоит.
  Вернуться, если наберётся статистика, где стоп стоит дальше уровня слома
  (там разница между политиками может быть больше).
- **Статус:** закрыто по замеру.

## 15. Условие входа вместо лимита съедает четверть планов

- **Замечено:** 23.09.2026.
- **Суть:** `now` (лимит ставится сразу) не использован НИ РАЗУ из 75
  принятых планов. Модель всегда вешает условие: sweep_reclaim 31,
  close_below 28, retest 11. Из-за этого 19 планов умерли с отказом
  «условие не наступило» и ещё 4 — «цель достигнута без входа».
  Логическая ловушка: sweep_reclaim выполняется, когда цена уже ушла от
  зоны входа вверх, а лимит стоит внизу — нужен третий заход.
- **Измерить:** что принесли бы те 19 планов, если бы лимит стоял сразу
  (по свечам: налился бы вход, дошла бы цена до цели или до стопа).
- **Измерено 23.09.2026** по 71 принятому плану (симуляция по 5м свечам:
  лимит стоит с момента разбора, живёт 12 ч, наблюдение 48 ч, издержки
  0.125% цены вычтены, пара занята пока позиция открыта плюс кулдаун 4 ч):

  | | с условием (как было) | лимит сразу |
  |---|---|---|
  | стало сделками | 12 | **27** |
  | сумма | **−8.57R** | **+7.40R** |
  | доля прибыльных | 1 из 12 (8%) | 10 из 27 (37%) |
  | исходы | | стоп 17, цель 6, по времени 4 |

  Осторожная оценка: четыре сделки закрылись на 48-часовой отметке, три из
  них в плюсе (+1.00, +1.53, +3.56); если считать их нулём — **+1.3R**
  вместо +7.4R. Сумма «было» совпадает с журналом до копейки (−8.57R), то
  есть сопоставление планов со сделками верное.
  Вывод: условие отсеивало в основном ВЫИГРЫШНЫЕ входы — после
  подтверждения цена уже ушла от зоны и обратно не возвращалась.
- **Сделано 23.09.2026.** В промте `now` — значение по умолчанию; условие
  только с доводом: старший ТФ против, пустой рынок, вход дальше дневного
  размаха от цены.
- **Статус:** закрыто, смотрим долю планов, доживших до заявки.
  ОГОВОРКА: замер на 71 плане за четверо суток одного режима рынка. Это
  довод «поменять», а не доказательство преимущества.

## 16. Горизонт наблюдения короче жизни позиции

- **Замечено:** 23.09.2026.
- **Суть:** `llm_outcomes` закрывал наблюдение через 48 часов, а позиция ИИ
  живёт до 336. «Цель не достигнута» у медленных планов означало «мы не
  досмотрели». По этим же числам принимаются решения о правилах.
- **Сделано 23.09.2026:** HORIZONS = (1, 4, 12, 24, 48, 72, 168) — неделя.
  Добавлены колонки `pct_48h`, `pct_72h`, `pct_168h` (первая писалась и
  раньше, но в COLUMNS её не было и она молча терялась).
- **Статус:** закрыто. Прежние строки остаются 48-часовыми — сравнивать
  доли цели до и после 23.09 напрямую нельзя.

## 17. Вероятность и ожидание — по своим исходам

- **Замечено:** аудит 22.09.2026, сделано 23.09.
- **Суть:** модель ставила `p` = 0.65 в 41 плане из 68; с такой константой
  EV = p·RR − (1−p) − издержки всегда выходил положительным, и отказ
  «ожидание не положительно» не сработал ни разу за 179 разборов. Реальная
  доля планов, дошедших до цели раньше стопа, — **0.19 на 47 наблюдениях**.
- **Сделано:** `llm_decide.empirical_p()` считает долю по нашим исходам с
  даты смены правил (`RULES_SINCE`); пока наблюдений меньше 30, EV не
  считается вовсе, ворота молчат, а в плане и в Telegram пишется «EV не
  считается: своих исходов N из 30». `p` модели остаётся в журнале как её
  мнение и на решение не влияет.
- **Статус:** закрыто; ворота включатся сами, когда наберётся 30 исходов.

## 18. Где в зоне ставить лимит — сигнала нет

- **Замечено:** 23.09.2026, замер.
- **Что мерили:** вход сдвигался от названного моделью уровня на ±0.2…0.6
  ATR (навстречу цене и глубже в зону), стоп и цель не трогали.

  | вход | сделок | сумма R | прибыльных |
  |---|---|---|---|
  | навстречу цене 0.4 ATR | 28 | +8.39 | 46% |
  | навстречу цене 0.2 ATR | 28 | +11.49 | 46% |
  | как назвала модель | 27 | +7.37 | 37% |
  | глубже 0.2 ATR | 27 | +10.85 | 37% |
  | глубже 0.4 ATR | 26 | +8.73 | 31% |
  | глубже 0.6 ATR | 25 | +11.07 | 28% |

- **Вывод:** разброс 7.4…11.5R на 25–28 сделках — это шум, зависимости от
  глубины не видно. Доля наливов почти не меняется (25–28 из ~70): сдвиг
  на 0.4 ATR ничего не решает, потому что за 12 часов цена ходит намного
  дальше. **Ничего не меняем**, вернуться при сотне сделок.
- **Статус:** закрыто по замеру (сигнала нет).

## 19. Что модель добавляет поверх механического правила

- **Вопрос владельца 23.09.2026:** если стоп, вход и цель зажаты кодом,
  что остаётся ИИ и окупается ли он.
- **Замер 1 — модель против механики** на 131 моменте с таблицей уровней.
  Механика: направление по тренду 4ч, вход — ближайшая живая зона между
  ценой и сломом (нет зоны — по рынку), стоп за сломом 1ч, цель — ближайший
  встречный пул, дальше те же ворота.
  Механика дала **9 планов и 4 сделки** (−1.25R), модель — 41 план и 17
  сделок (+17.27R). **Сравнение не состоялось**: четыре сделки не выборка.
  Но причина показательна: у механики 80 моментов из 131 отсеялись по
  «низкому отношению» — от честного стопа до БЛИЖАЙШЕГО пула R:R < 2.
- **Замер 2 — чем отличается план модели** (41 план):

  | | модель | механика |
  |---|---|---|
  | цель — ближайший пул | 3 из 41 | всегда |
  | R:R первой цели, медиана | **3.10** | 1.02 |
  | вход дешевле рынка, медиана | **0.59 риска** | 0 (по рынку) |

- **Замер 3 — не вредит ли дальняя цель.** Половина позиции на ближайшем
  пуле, остаток до цели модели, стоп после первой цели в безубыток:
  **−2.31R против +17.40R** у одной дальней цели. Доля прибыльных выше
  (61% против 53%), сумма — хуже: ранний тейк режет прибыль. Дальняя цель
  у модели ПРАВА; в аудите 22.09 я записал её в дефекты — это было неверно,
  дефектом был стоп в шуме, а не цель.
- **Замер 4 — безубыток при +1R на новой геометрии:** +6.58R против
  **+7.19R** без него, доля прибыльных 30% против 37%. То есть он слегка
  мешает. На реальных сделках 19–21.09 он, наоборот, давал +4.25R.
  Объяснение: безубыток спасает, когда стоп стоит в шуме и до цели сделка
  всё равно не доходит; когда геометрия честная — он клипает возвраты,
  которые потом доходят до цели. Обе выборки малы (27 и 11), разница в
  новой — 8%. **Оставляем как есть**, перемеряем на 30 живых сделках.
- **Статус:** вопрос «окупается ли модель» ОТКРЫТ. Симуляции на четырёх днях
  растущего рынка дают +17R, живая торговля за предыдущие четверо суток дала
  −8.57R. Честный ответ даст сравнение депозитов LLM и SMC за месяц: SMC —
  тот же метод механическим кодом, её отпустили 21.09 (предел издержек).

## 20. Навык входа подтверждён, навык направления — нет

- **Замер 23.09.2026** на всех принятых планах 19–23.09, три политики,
  одна симуляция, издержки вычтены:

  | политика | сделок | сумма R | прибыльных |
  |---|---|---|---|
  | A. план модели целиком | 27 | **+6.36** | 37% |
  | B. её сторона и геометрия, вход ПО РЫНКУ | 38 | **−10.67** | 26% |
  | C. ВСЕГДА LONG по рынку, та же геометрия | 34 | **+23.29** | 50% |

- **A против B = +17R** — это цена её выбора входа: те же сделки в тех же
  направлениях без её лимита дают минус. Навык подтверждён, он крупный.
- **C против A** — тупое «всегда лонг» обыграло её отбор втрое. Согласуется
  с живой торговлей: 19–20.09 модель дала 29 шортов в растущий рынок
  (−8.57R), 21–23.09 развернулась в лонги — рынок продолжил расти. Оба раза
  она была не на той стороне сноса.
- **ОГОВОРКА, без которой вывод неверен:** пять дней одного направления
  рынка (альты +15% за неделю). В таком окне «всегда лонг» обязано
  выигрывать, и это не говорит ничего о падении. Симметрично: её шорты
  19–20.09 в падающем рынке выглядели бы отлично.
- **Гипотеза на проверку:** если через месяц навык направления так и не
  проявится, а навык входа подтвердится — направление брать механически
  (тренд 4ч, как у SMC), а модель оставить на вход и цель внутри него.
- **Как проверить без симуляций:** сравнение депозитов LLM и SMC за месяц
  через рост и падение. Обе торгуют тем же методом на тех же парах, одна
  через модель, другая механическим кодом.
- **Статус:** открыто, ждём месяц живой работы.

## 21. Автономия: счёт модели, порядок разбора, ворота как диалог

- **Запрос владельца 23.09.2026:** меньше ограничивать кодом, дать модели
  самой находить сетапы и верно определять направление.
- **Что нашли перед правкой:** модель не слепо следует данным — 23 плана из
  77 против структуры 4ч, и именно они убыточны (−2.99R против +9.17R).
  На 20.09 четырнадцать шортов из двадцати пяти были против 4ч. Причина не
  в данных и не в коде: **у модели нет памяти между вызовами**, она видит
  два прошлых разбора этой пары и не может знать, что её контртрендовые
  планы дают два попадания из десяти.
- **Сделано:**
  1. **Счёт модели в разметке** (`llm_outcomes.scoreboard`): сколько её
     планов дошло до цели раньше стопа, в разрезе стороны, согласия со
     структурой 4ч и условия входа. Это факт о себе, а не правило: правило
     можно не применить, счёт — нет. В наблюдения добавлена колонка
     `htf_trend`, без неё разрез по тренду не считался.
  2. **Порядок разбора сверху вниз** в промте: день и 4ч → фон рынка
     (USDT.D) → 1ч → где спрятана ликвидность → и только потом «торговать
     ли вообще».
  3. **USDT.D одной фразой** («ОРИЕНТИР РЫНКА»): блок «рынок в целом»
     цитировался в одном разборе из десяти — двадцать чисел про
     капитализации модель не читала. Теперь вывод: растёт доля стейблов —
     деньги уходят из риска, лонг альта идёт против фона.
  4. **Ворота как диалог** (`REVISABLE_GATES`): отказ по суждению
     возвращается модели с причиной, и она перестраивает план — один раз.
     За 206 разборов код отказал 77 раз; это 77 выброшенных
     пятнадцатиминутных разборов. Переделка дёшева: вопрос и разметка уже в
     кэше сервера.
  5. **График в Telegram — рабочего таймфрейма.** `chart_frame.pick`
     возвращал 15m или 4h, часового варианта не было вовсе: человек видел
     один масштаб, бот работал в другом. Теперь всегда часовой, шире окно
     для размашистых планов.
- **Место в окне** под это освободили по замерам: уровней 20 → 12 (L13+
  встречались в полях плана 14 раз из 123), свечей 12 → 8, объём и дельта
  у уровня печатаются только когда заметны, ответ 2400 → 2300 токенов.
  Живая разметка ETH: вопрос 8994 токена при окне 12288 (было 9233).
- **Что мерить дальше:** доля планов `now` (была 0 из 5 после первой
  правки промта — модель инструкции не послушалась); доля планов против
  тренда 4ч; сколько переделок заканчивается принятым планом.
- **Статус:** сделано, смотрим воронку.

## 22. Порог R:R, который можно понизить сильным контекстом

- **Идея (совет со стороны, 23.09.2026):** разрешить сетап с R:R ниже
  порога, если контекст явно за него — пробой ключевого уровня, фандинг,
  новости. Сейчас порог жёсткий и одинаковый для всех планов.
- **Почему сейчас нельзя:** чтобы платить за понижение порога, нужна
  калиброванная вероятность. У модели `p` — константа 0.65 в 41 плане из
  68, фактическая доля цели раньше стопа 0.19. Понижать порог по её слову
  значит понижать его всегда.
- **Когда станет можно:** после того как `llm_decide.empirical_p` наберёт
  30 исходов по нынешним правилам и заработает. Тогда порог можно сделать
  функцией класса плана: там, где наш собственный счёт показывает долю
  выше базовой, R:R 2.0 можно опустить до 1.7 — и это будет обосновано
  числом, а не красноречием.
- **Измерить:** долю цели раньше стопа по классам (по тренду / против;
  с условием / лимитом сразу; по парам) — те же разрезы, что в счёте
  модели. Порог опускать только там, где класс держит долю ≥ 1/(1+R:R).
- **Статус:** отложено до накопления исходов.

## 23. Советы со стороны: что проверено и отклонено

- **23.09.2026** разобраны два внешних совета по архитектуре (конвейер из
  нескольких запросов к модели; «торговый устав» в промте; отдельный
  критик MiniCrit-1.5B; раздел «математика» в рассуждении).
- **Принято и уже сделано:** возврат плана модели на переделку с причиной
  (совпало с нашей правкой того же дня); режим рынка считать кодом, а не
  спрашивать у модели; урезание контекста до читаемых блоков.
- **Отклонено по замерам:**
  - «код находит сетап, модель валидирует» — механическое правило дало
    9 планов и 4 сделки на 131 моменте: валидировать нечего (п. 19);
  - цепочка из трёх запросов — один запрос 15.6 минуты медианы, цепочка
    даёт разбор пары раз в 18 часов;
  - «модель не знает правил» — знает и не следует: XRP сама написала, что
    за L10 идея мертва, и поставила стоп на L7; после правки «по умолчанию
    now» — ноль `now` из пяти планов;
  - R:R 1:3 — с честным стопом порог 2.5 уже требует цели дальше дневного
    размаха у 15 пар из 21;
  - раздел «математика» в рассуждении — арифметика в мысли уже есть у 39%
    разборов и съедает бюджет (LTC 21.09: семьдесят строк перебора);
    галлюцинаций R:R нет вовсе, его считает код по названным уровням.
  - MiniCrit-1.5B — модель с таким именем не подтверждена; прежде чем
    планировать, проверить существование.
- **Статус:** записано, чтобы не разбирать те же советы заново.

## 24. USDT.D: пересказ хода рынка, а не прогноз

- **Идея владельца 23.09.2026:** раз в 12 часов разбирать график доминации
  USDT и от него строить взгляд на весь рынок.
- **Измерено по своему ряду** (508 точек, 21–23.09, шаг ~5 мин):

  | окно | связь USDT.D с ОДНОВРЕМЕННЫМ ходом рынка | с БУДУЩИМ ходом |
  |---|---|---|
  | 4 ч | **−0.83** | +0.13 |
  | 12 ч | **−0.92** | −0.04 |

  Механика простая: USDT.D = капитализация USDT / вся капитализация. За эти
  двое суток капитализация USDT изменилась на +0.05%, то есть стояла, —
  значит доля двигалась чисто обратно капитализации рынка. Это другая
  запись фразы «рынок вырос», которую модель и так видит в ходе цены.
- **Что несёт информацию:** не доля, а КАПИТАЛИЗАЦИЯ USDT. Растёт —
  напечатали стейблов, на рынок пришли деньги, которым предстоит что-то
  купить. Стоит — просто переложились. Поле `usdt_cap_24h` добавлено в
  факты, формулировка в разметке построена от притока, а не от доли.
- **Решение по идее:** отдельный разбор USDT.D раз в 12 часов НЕ делать.
  Две причины: (1) предсказательной силы в наших данных нет; (2) общий
  взгляд на рынок, применённый ко всем парам, размножит одну ошибку
  направления на все сделки сразу — а навык направления у модели пока не
  доказан (п. 20). Данные остаются в разметке, решение — по паре.
- **ОГОВОРКА:** ряд — двое суток растущего рынка. Вывод «не предсказывает»
  может не пережить месяца с падением. Ряд копится, замер повторить, когда
  в нём будет падение и заметная эмиссия.
- **Статус:** идея отклонена по замеру, данные улучшены, перепроверить
  через месяц.

## 25. Быстрая модель-сканер перед основной: слой есть, он код, и он работает

- **Идея владельца 23.09.2026:** поставить маленькую быструю модель «под
  трейдинг», чтобы она пробегала все пары на 15м/1ч/4ч/дне, находила сетапы
  по косвенным признакам и отдавала кандидатов основной модели на решение.
- **Слой уже стоит и он не модель, а формула.** `llm_urgency.py` каждый цикл
  считает повод по всем парам: свежий слом (BOS/CHoCH), всплеск ликвидаций
  против медианы часа, цена у уровня, скачок ОИ. Пара без повода модели не
  отдаётся вовсе, кроме планового прохода раз в 8 часов.
- **Замер эффективности фильтра** (212 разборов 19–23.09, отметки срочности
  из журнала сопоставлены с исходом разбора):

  | | разборов | кончилось планом | модель ничего не нашла |
  |---|---|---|---|
  | пара с поводом (≥3 балла) | 158 | **45%** | 11% |
  | пара без пометки | 54 | **13%** | 50% |

  Разделение в 3.5 раза. По виду повода: CHoCH — 60% планов, цена у уровня —
  49%, BOS — 48%, ликвидации — 47%, ОИ — 47%.
- **Мест для новых кандидатов нет.** За четверо суток 840 циклов из 879
  (96%) застали модель занятой; пустых, где она свободна и отдать нечего, —
  39. Медиана разбора 18.9 мин, потолок ~53 разбора в сутки, и мы в него
  упираемся. Найти вдвое больше сетапов — удлинить очередь, в которой
  разметка кандидата протухает до его очереди.
- **Потолок выигрыша даже у идеального фильтра — 21%.** Из 212 разборов
  только 44 — «модель посмотрела и ничего не нашла». Ещё 90 (42%) — план
  модель построила, а завернул код (обоснование не о том плане 32, вход на
  пуле стопов 9, стоп в скоплении 8, план против структуры 3). Вторая
  группа — брак геометрии, предфильтр её не трогает.
- **Цена второй модели:** llama-server держит 18.6 ГБ из 22 ГБ ОЗУ и ~5 ядер
  из 8. Отобранные ядра — прямо минус разборы в сутки; чтобы окупиться,
  фильтр должен экономить больше своего потолка в 21%.
- **Модели «под трейдинг» под эту задачу нет.** Финансовые дообучки (FinGPT
  и подобные) учены на финансовом ТЕКСТЕ — новости, отчётность, настроения,
  не на OHLC. Задача фильтра — арифметика сравнения (этот минимум ниже
  предыдущего или нет), где формула точна и воспроизводима, а маленькая
  модель может «увидеть» слом, которого нет.
- **ЧТО ИЗ ИДЕИ ВЗЯТЬ — таймфреймы.** Срочность считается ТОЛЬКО по часу:
  `llm_urgency._fresh_break` и `_near_level` читают `context.frames['poi']`,
  а `TF_POI = '1h'`. Кадр 4ч рядом загружен и не используется, 15м и день не
  загружаются вовсе. Мы слепы к слому на 4 часах и к касанию дневного
  уровня — при том что CHoCH самый доходный повод, а на старшем ТФ он весит
  больше. Даст не «больше сетапов» (места нет), а лучший ПОРЯДОК тех же ~53
  разборов в сутки.
- **Чем мерить, когда возьмёмся:** та же таблица «повод → доля планов», но с
  разбивкой по таймфрейму повода. Вес старшего ТФ поднимать, только если
  доля планов у него выше часовой.
- **Статус:** вторая модель отклонена по замеру. Многотаймфреймовая
  срочность — принята, отложена до конца замера шести правок 21–23.09
  (иначе нечем будет отделить её эффект). Вернуться после 25.09.2026.

## 26. `now` по умолчанию: счёт начинается 23.09.2026 10:04 UTC

- **Что сделано:** значение по умолчанию для условия входа — `now` (лимит
  ставится сразу), условие — только с доводом. Коммит `d60b4ba`, выкачено
  23.09 в 10:04 UTC.
- **ОСТОРОЖНО СО СЧЁТОМ.** 23.09 я посчитал «0 планов из 27 выбрали `now`»
  и сделал вывод, что модель не слушается. Счёт был неверный: он брал
  планы с 21.09, то есть написанные ДО того, как указание появилось. После
  выкатки планов на тот момент было **ноль**. Вывод снят.
- **Чем мерить:** доля планов с `trigger = now` среди планов с `at >=
  2026-09-23T10:04`. Порог объявлен заранее: **меньше 20% за три дня —
  условие входа переносим в код** (замер 71 плана: с условием дожили до
  сделки 12 и дали −8.57R, с лимитом сразу — 27 и +7.40R).
- **ЕСЛИ `now` НЕ ПОЯВИТСЯ — смотреть сюда, а не на упрямство модели.**
  В промте два правила ОБЯЗЫВАЮТ поставить условие, и одно само называет
  себя частым:
    - `llm_prompt.py:122` — «СТАРШИЙ ТФ ПРОТИВ bias 1ч (самое частое
      противоречие): план только с условием по закрытию или ретесту, не
      «now»»;
    - `llm_prompt.py:124` — «ПУСТОЙ РЫНОК: … только условие по закрытию
      свечи».
  Если эти два исключения покрывают большую часть разборов, значение по
  умолчанию не срабатывает никогда, и виновата не модель. Померить долю
  разборов, где исключения применимы, пока нечем: колонка `htf_trend` в
  `llm_outcomes.csv` добавлена, но старые строки её не имеют — считать с
  того дня, как она наполнится.
- **Статус:** наблюдение, считать с 25.09.2026.

## 27. Карта ликвидаций: вес — оборот (сделано 23.09, эффект не измерен)

- **Что сделано:** `llm_market.liquidation_estimate` весит ОБОРОТОМ свечи,
  а не приростом ОИ, и считает все свечи с живым рядом, а не только те, где
  ОИ вырос. Коммит `9ee0048`.
- **Замер геометрии** (332 точки, 10 пар, 19–23.09; карта строится в момент
  t, сверяется с настоящими ликвидациями следующих 4 часов):

  | | поймано | покрытие | превышение | молчит |
  |---|---|---|---|---|
  | было (прирост ОИ) | 34.8% | 7.9% | 4.40 | 61% |
  | стало (оборот) | **52.9%** | 18.8% | 2.81 | **44%** |

  Лучше у 10 пар из 10 — от BTC (44.5→58.0%) до SOL (28.2→74.9%).
- **ЧЕГО ЗАМЕР НЕ ПОКАЗЫВАЕТ: пользы.** Геометрия карты улучшилась, но
  вопрос был не в ней, а в том, что модель ссылалась на блок ликвидаций
  лишь в **17% разборов**. Мерить надо именно это число после 25.09 — и
  отдельно то, не начнёт ли модель чаще ставить цель на ликвидационный
  кластер И промахиваться (цель не дальше дневного размаха, п. 0 в
  `ai_strategy.md`).
- **Оговорка:** 332 точки за четверо суток ОДНОГО растущего рынка.
  Для вывода «оборот лучше ОИ» этого хватает (10 пар из 10), для выбора
  ширины карты — нет. Повторить на недельном ряду.
- **Отклонено по замеру:** малые плечи (Model 2 у CoinGlass — 2x/3x/5x)
  дают 41.4% вместо 52.7%: при плече 5 ликвидация в 20% от цены, за часы
  плана туда не доходят. Набор 10/25/50/100 совпал с их Model 1
  независимо.
- **Статус:** правка выкачена, эффект мерить с 25.09.2026.

## 28. Заявка не помнит, как она появилась (пачка после 25.09)

- **Дыра 1: переход «условие наступило → заявка выставлена» невидим.**
  План найден — сообщение есть; план отклонён — есть; план или заявка
  сняты — есть (с 23.09); сделка открыта и закрыта — есть. А момент, когда
  взведённый план дождался условия и на биржу ушёл лимит, не сообщается
  никому: `limit_order_placed` в `telegram_notify` существует, но зовёт её
  только боевой счёт (`trade_manager.py:1220`), фантомный брокер ставит
  заявку молча. Между «сетап найден» и «сделка открыта» может пройти 12
  часов тишины, и по ней нельзя отличить «план ещё ждёт условия» (денег в
  рынке нет) от «заявка стоит» (позиция может налиться в любую секунду).
  После добавления сообщения о снятии история стала рваной: «сетап найден»
  → тишина 12 ч → «сетап снят», а что заявка существовала — ни слова.
- **Дыра 2: по стоящей заявке не видно, из `now` она или из условия.**
  В момент плана это видно — в подписи есть строка «⏳ Условие: …»
  (`telegram_notify.py:418`, семь формулировок в `TRIGGER_TEXT`). Но
  `strategy_llm._reshape` не переносит `trigger_when` в сигнал, поэтому
  заявка не помнит своего происхождения, и в разделе «📥 Заявка стоит,
  ждёт цену» обе выглядят одинаково.
- **Что сделать:** перенести `trigger_when`/`trigger_level` в сигнал и в
  запись заявки; показывать в карточке «поставлена сразу» либо «по
  условию: …, наступило в HH:MM»; послать сообщение в момент постановки
  лимита по сработавшему условию (для `now` это уже покрыто сообщением о
  сетапе). Около десяти строк в трёх местах плюс тесты.
- **Статус:** отложено в пачку после 25.09.2026 вместе с п. 25
  (многотаймфреймовая срочность).

## 29. Стоп срабатывает по фитилю, а структура ломается по закрытию

- **Случай 23.09.2026, DOGEUSDT #56, −1.08 R на живой идее.** Модель
  написала: «если цена пробьет L10 (0.09924), идея 1ч CHOCH и защиты OB
  мертва». Стоп код поставил за уровень, на 0.098788. Свечи:

  ```
  08:00  O 0.10084  H 0.10168  L 0.09869  C 0.09967   ← фитиль забрал стоп
  09:00  O 0.09967  H 0.10027  L 0.09951  C 0.09984
  10:00  O 0.09984  H 0.10008  L 0.09905  C 0.10007
  11:00  O 0.10007  H 0.10018  L 0.09880  C 0.09933
  ```

  Ни одна свеча не ЗАКРЫЛАСЬ ниже 0.09924. Идея по определению самой
  модели была жива всё время; нас выбило фитилём. Буфер под уровнем — 0.45%,
  фитиль сходил на 0.55%.
- **Суть расхождения — в нашем же коде:** слом структуры считается ПО
  ЗАКРЫТИЮ (`BREAK_ON_CLOSE`), а стоп исполняется ПО КАСАНИЮ. Мы говорим
  модели «структура ломается закрытием» и ставим стоп, который снимается
  тенью.
- **Два способа починить, оба требуют замера:**
  1. увеличить буфер за уровнем инвалидации (дешевле, логику не меняет, но
     удлиняет стоп и бьёт по R:R и по доле издержек);
  2. выходить по ЗАКРЫТИЮ часа за уровнем, а не по касанию (честнее, но
     замер 22.09 дал всего +0.05R на событие — надо повторить на новой
     геометрии стопа).
- **ВНИМАНИЕ, ЭТО ОБЩИЙ СЛОЙ.** Буфер стопа читают все пять стратегий.
  Правка делается отдельно, с отпечатками решений каждой стратегии до и
  после (`tests/test_strategy_isolation_behaviour.py`).
- **Статус:** записано 23.09.2026, к работам после 25.09.

## 30. НОЛЬ планов за ночь: ворота сложились в пустое множество

- **Замер ночи 23.09 15:00 → 24.09 04:42 UTC (13 разборов):**

  | | |
  |---|---|
  | планов принято | **0 из 13 (0%)** |
  | было 22–23.09 | 21 из 64 (33%) |
  | переделок | 10 из 13 |
  | переделок, кончившихся планом | **0 из 10** |

  Вероятность получить 0 из 13 при прежней доле 33% — около 0.5%. Это не
  невезение.
- **Ворота за ночь:** стоп внутри живой зоны 4, план против структуры 3,
  мало конфлюенса 3, стоп в скоплении стопов 2, модель пропустила 1.
- **ГЛАВНАЯ ПРИЧИНА — живые зоны занимают треть цены.** Замер 24.09 по 10
  парам: незакрытые зоны (имбалансы 1ч + зоны 4ч) покрывают в среднем
  **34.9%** полосы ±3% от цены, у DOGE 81.6%, у BTC 75.1%, у ARB 0%.
  Ворота `stop_inside_live_zone` с 23.09 читают и зоны 4ч (коммит
  `4a7450c`), а зоны 4ч широкие: у DOGE имбаланс 0.09323..0.09869 — это
  5.5% цены. Стоп обязан быть за сломом структуры И вне зоны И не в пуле
  стопов И не теснее минимума И давать R:R ≥ 2 И укладываться в предел
  издержек. На части пар такого места просто нет.
- **ПЕРЕДЕЛКА НЕ РАБОТАЕТ, И ВИДНО ПОЧЕМУ.** Модель не берёт другой
  уровень — она подвигает число:
  - ZEC: стоп 1578.92 → после замечания 1595.37, обе внутри той же зоны
    1570.69..1602.14;
  - ARB: 0.223871 → 0.223854, сдвиг на 0.008%.
  Текст отказа прямо говорит «ставить за 1602.14», и модель этого не
  делает. Проверить: есть ли названная граница в таблице уровней вообще, и
  не получается ли, что единственный законный стоп даёт R:R ниже порога
  (у DOGE граница 0.09869 — это +5.6% от цены).
- **ЦЕНА ПЕРЕДЕЛОК.** 10 переделок за ночь, 0 результата. Разбор с
  переделкой — 36.7 мин против 32.1 без. За ночь на переделки ушло около
  часа чистого времени модели при нулевом выходе. Если после 25.09 доля
  удачных переделок останется нулевой — выключать.
- **ОТДЕЛЬНЫЙ БАГ ГРАНИЦЫ.** LTCUSDT 23.09 19:48: «вход 63.03 выше слом
  структуры 1ч 63.03» — одно и то же число. `plan_against_structure`
  сравнивает `entry >= level` для шорта, поэтому вход РОВНО на уровне
  слома всегда отказ. При этом уровень слома лежит в таблице с
  максимальным весом и подписан «слом структуры» — модель естественно
  выбирает его. Ловушка нашей же постройки.
- **Статус:** разобраться первым делом после 25.09. Сначала замерить, на
  скольких парах множество допустимых стопов пусто, и только потом решать,
  какие ворота ослаблять.

## 31. Модель тормозит сама себя: веса не помещаются в память

- **Замер 24.09 04:47 UTC, llama-server работает 17.9 ч:**

  ```
  RssAnon   8.93 ГБ     KV-кэш, буферы, перепаковка весов
  RssFile   8.96 ГБ     веса модели, лежащие в памяти
  файл модели         15.9 ГБ
  ```

  **В памяти только 8.96 ГБ весов из 15.9 ГБ.** Остальные 7 ГБ вытеснены и
  дочитываются с диска: `vmstat` показывает 13–28 МБ/с чтения непрерывно,
  при полностью пустом свопе.
- **Следствие — скорость упала на треть:** 2.0–2.6 ток/с днём 23.09 →
  **1.43–1.58 ток/с** ночью. Разбор вырос с 21 до 32 минут, пропускная
  способность с ~2.2 до ~0.95 разбора в час.
- **Причина не в наших правках.** Вопрос не вырос (медиана 8889 токенов до
  правки карты, 8967 после), бот занимает 0.23 ГБ. Растёт анонимная память
  самого llama-server: в 14:00 система показывала used 7.7 ГБ, в 04:47 —
  10.1 ГБ, и на столько же усох страничный кэш, где живут веса.
- **Что проверить:** (1) не дублирует ли llama.cpp веса при перепаковке под
  CPU — тогда помогает отключение перепаковки или `--no-mmap`; (2) сколько
  на деле занимает KV-кэш при `-c 12288`; (3) помогает ли периодический
  перезапуск llama-server (косвенно да: сразу после старта 2.1–2.6 ток/с,
  через 18 часов 1.5).
- **Это же отвечает на вопрос «почему не используем всю память».** Мы её
  не недоиспользуем, а ПЕРЕиспользуем: нужно 8.9 + 15.9 = 24.8 ГБ, а есть
  20 ГБ. Отсюда постоянное вытеснение.
- **РЕШЕНО 24.09.2026 08:28 UTC. Причина — рост анонимной памяти самого
  llama-server.** Перезапуск службы вернул всё на место:

  | | RssAnon | RssFile (весов в памяти) | чтение с диска | iowait | скорость |
  |---|---|---|---|---|---|
  | сразу после старта | 0.74 ГБ | **15.93 из 15.93 ГБ** | 108 бл/с | 0% | — |
  | через 21 час | 8.80 ГБ | 9.14 ГБ | 54 960 бл/с | 10% | 1.35 ток/с |
  | после перезапуска, первый разбор | 1.79 ГБ | **15.93 ГБ** | 0 бл/с | 0% | **2.23 ток/с** |

  Разбор снова 22 минуты вместо 34. Утечки в НАШЕМ коде нет: бот занимает
  0.23 ГБ, вопрос не менялся (8838–8967 токенов до и 8956 после).
- **Лечение — сторож `/opt/kraken/llm_guard.sh`, cron каждые 30 минут.**
  Порог: анонимная память выше 2.5 ГБ. Посчитан, а не угадан — весам нужно
  15.93 ГБ, пригодной памяти около 18.7 ГБ, значит анонимной остаётся
  меньше 2.8 ГБ. Перезапуск делается только когда вердикт забран циклом и
  начатый разбор моложе 240 с (та же защита, что у
  `restart_when_idle.sh`); не дождался за час — не трогает вовсе и приходит
  через полчаса. Журнал: `/opt/kraken/bot_data/llm_guard.log`.
- **ЧТО ЕЩЁ НЕ ЗНАЕМ:** откуда именно берётся рост. Подозреваемые —
  KV-кэш при `-c 12288`, буферы, черновик MTP (`--spec-type draft-mtp`).
  Скорость роста мерить по частоте записей в журнале сторожа: если
  перезапуск нужен чаще раза в три часа, искать причину, а не лечить
  симптом.
- **Статус:** симптом снят 24.09.2026; **причина найдена 25.09.2026 — кэш
  подсказок llama-server в ОЗУ, см. п. 45.** Числа скорости за ночь
  23–24.09 занижены и для сравнения «до/после» правок не годятся —
  сравнивать только после 24.09 08:28.

## 32. Законные стопы печатаются в таблице (сделано 24.09, эффект не измерен)

- **Что сделано.** `llm_decide.legal_stop_ids` считает, какие уровни годятся
  под стоп, ДО вопроса; `llm_context` печатает это строкой «ГОДЯТСЯ ПОД
  СТОП: лонг — L9, L10, L11, L12   шорт — L2, L3, L4, L5, L6» и меткой
  «← СТОП: ЛОНГ» у самих уровней. Если законных нет ни для одной стороны —
  разбор не начинается (`нет законного стопа`), модель не тратит полчаса.
  Плюс строгое сравнение в `plan_against_structure`: вход РОВНО на уровне
  слома больше не отказ (ретест, а не ошибка).
- **ЗАЧЕМ.** Ночь 23–24.09: ноль планов из тринадцати, десять переделок,
  ни одной удачной. Модель узнавала правила только из отказа и на переделке
  двигала четвёртый знак того же уровня. Теперь правило стоит рядом с
  уровнем.
- **ПОПРАВКА К ПЕРВОМУ ЗАМЕРУ.** 24.09 я сперва насчитал «1.1 законного
  стопа из 12, у 9 пар-направлений из 20 нет ни одного». Замер был неверен:
  он брал голую цену уровня вместо ОТСТУПЛЕННОГО стопа и не учитывал, что
  пул на самом названном уровне не мешает (`stop_in_liquidity(...,
  stop_level=...)`). По боевой функции:

  | | законных стопов |
  |---|---|
  | в среднем | около 3 из 12 |
  | пар без стопа для ЛОНГА | 2 из 10 |
  | пар без стопа для ШОРТА | 2 из 10 |
  | пар, где разбор не начнётся вовсе | **1 из 10** (DOGE) |

  То есть множество допустимых стопов НЕ пусто — просто модель выбирала
  вслепую один из двенадцати, имея около трёх верных. Ранний отказ сработает
  редко; главная польза — подсказка.
- **Чем мерить с 25.09:** доля принятых планов (ночью 0 из 13, до того 21
  из 64 = 33%); доля отказов по стопу («стоп внутри живой зоны», «стоп в
  скоплении стопов», «план против структуры») — должна упасть почти до
  нуля, иначе подсказка не читается; доля переделок (была 10 из 13).
- **Цена:** вопрос вырос примерно на 100 токенов из ~8900.
- **Статус:** выкачено 24.09.2026, эффект мерить с 25.09.

## 33. Слом 1ч против целой 4ч: эффект есть, но 7–10 пунктов

- **Идея владельца 24.09.2026:** час ломает структуру, 4ч цела — это ловушка
  ликвидности, цена вернётся. Если так, стоп плана «по тренду 4ч» должен
  стоять за слом 4ч, а не 1ч.
- **Замер:** 748 сломов структуры 1ч по 18 парам (структура строится теми же
  `smc.structure`, что и в боте), окно 12 часов после слома.

  | 4ч относительно слома 1ч | сломов | вернулось за уровень | ушло дальше |
  |---|---|---|---|
  | согласна | 462 | 63% | 37% |
  | ПРОТИВ | 286 | **73%** | 26% |

  Уточнённая метрика — глубина выноса до возврата:

  | | медиана | 90-й проц. | выбило стоп и вернулось |
  |---|---|---|---|
  | согласна | 1.77% | 5.59% | 60% |
  | ПРОТИВ | 1.75% | 4.99% | **67%** |

- **ЧТО ЭТО ЗНАЧИТ.** Направление подтвердилось: при несогласной 4ч цена
  возвращается чаще. Но перевес всего 10 пунктов по возврату и 7 по
  «выбило и вернулось», а глубина выноса ОДИНАКОВА (1.75 против 1.77%).
  И главное: возврат за уровень в 12 часов — норма даже при согласной 4ч
  (63%), то есть сам по себе он ловушку не выделяет.
- **Решение:** стоп на слом 4ч НЕ переносить. Семь пунктов не окупают
  кратного роста стопа: на SOL 24.09 дальний стоп уронил бы R:R с 2.0 до
  1.26, то есть план просто перестал бы существовать.
- **Что сделать вместо:** назвать расхождение ФАКТОМ в разметке. Оба уровня
  (`слом структуры 1ч` и `слом структуры 4ч`) уже лежат в таблице с
  максимальным весом, но нигде не сказано, согласны они или нет. Одна
  строка, которую считает код.
- **15 минут не грузить:** если на 4ч перевес 10 пунктов, на 15м он будет
  меньше, а кадр стоит запросов и места в окне.
- **Статус:** измерено 24.09.2026, вывод получен, строку про расхождение —
  в работы после 25.09.

## 34. Буфер стопа: 0.35% — примерно верно, расширять не надо

- **Откуда вопрос.** П. 29: DOGE 23.09 выбило фитилём при живой идее.
  Напрашивалось «буфер мал, расширить».
- **ПЕРВЫЙ ЗАМЕР БЫЛ НЕ О ТОМ.** 24.09 я померил вынос за СЛОМАННЫЙ
  уровень и получил медиану 1.75% против буфера 0.33% — «в пять раз мало».
  Уровень не тот: стоп ставится за уровень ИНВАЛИДАЦИИ (последний
  подтверждённый HL/LH), он дальше.
- **Правильный замер** (1738 наблюдений «идея жива», 962 «структура сломана
  закрытием», 18 пар, окно 12 ч; идея жива = ни одного ЗАКРЫТИЯ за уровнем,
  как и считает наш `BREAK_ON_CLOSE`):

  | глубина прокола при ЖИВОЙ идее | |
  |---|---|
  | таких случаев | 245 из 1738 (14%) |
  | медиана | 0.32% |
  | 90-й проц. | 0.92% |
  | 95-й проц. | 1.21% |

  | буфер | выбивает живых идей |
  |---|---|
  | **0.35% (наш)** | **7%** |
  | 0.50% | 5% |
  | 0.75% | 3% |
  | 1.00% | 1% |

- **ВЫВОД: расширять невыгодно.** Переход с 0.35% на 0.75% спасает 4% сделок
  из ста, но удлиняет стоп на 0.4 п.п. При типичной дистанции стопа около 2%
  это примерно −17% к R:R на КАЖДОЙ сделке. Четыре процента спасённых не
  окупают семнадцати процентов потерянного отношения на всех.
- **DOGE попал в семипроцентный хвост**, а не вскрыл системную ошибку.
- **Что остаётся от п. 29:** второй вариант — выходить по ЗАКРЫТИЮ часа за
  уровнем, а не по касанию. Замер 22.09 дал +0.05R на событие; повторить на
  новой геометрии стопа.
- **Статус:** «расширить буфер» закрыто замером 24.09.2026. Выход по
  закрытию — перепроверить после 25.09.

## 35. Разбор всех 17 сделок ИИ: откуда 15 стопов из 17

Замер 24.09.2026 по `paper_trades.csv` (все закрытые сделки стратегии) и
`llm_calls.csv`. Структура 4ч и дня считается теми же `smc.structure`, что
и в боте, на момент ОТКРЫТИЯ каждой сделки.

**Итог стратегии: 17 сделок, 15 стопов (88%), 2 цели, −9.53 R (−475 USD).**

### Причина 1 — встречная торговля. В ней весь убыток

| | сделок | выигрышных | итог |
|---|---|---|---|
| ПРОТИВ тренда 4ч | 6 | **0** | **−6.32 R** |
| по тренду 4ч | 11 | 2 | −3.21 R |

По ДНЕВНОМУ тренду ещё резче: 16 сделок из 17 открыты при бычьем дне, и
8 из них — шорты. Восемь встречных шортов, ноль выигрышей, около −8.4 R.
Девять согласных сделок в сумме дали −1.1 R. По стороне: 9 шортов — 0
выигрышей, 8 лонгов — 2. Рынок за период вырос (BTC +2.1%, ETH +0.7%).

**Модель НАЗЫВАЛА конфликт и всё равно открывала:** SUI «старший порядок
вверх», SHIB «внутри восходящего тренда 4ч/Дня», DOT «4ч вниз, день
вверх», UNI «Тренд вверх, цена в премиуме ноги».

**ЧЕГО НЕ УЧИТЫВАЛИ: в коде нет проверки стороны плана против старшего
тренда.** `plan_against_structure` сверяется только со сломом структуры на
ЧАСЕ. В промте просьба есть («старший ТФ против bias 1ч → только с
условием, p не выше 0.55»), но просьбы эта модель не исполняет — четыре
доказанных случая (стоп XRP, `now`, переделка, подсказка по стопам).

### Причина 2 — отданные прибыли

| | |
|---|---|
| стопов, побывавших на +1R и выше | **6 из 15** |
| стопов, побывавших на +0.5R и выше | 9 из 15 |
| медиана лучшего хода у стопнутых | +0.81 R |
| **итог, если бы безубыток на +1R работал у всех** | **+2.06 R вместо −9.53 R** |

Безубыток включён с 23.09 и механика верная (стоп переносится при
достижении `be_level` = вход ± 1R, а не после цели — `paper_broker`
строка ~1392). Ни одна сделка после включения до +1R пока не дошла.

### Причина 3 — протухшие заявки

| ожидание налива | сделок | выигрышных | итог |
|---|---|---|---|
| меньше часа | 10 | 2 | −2.13 R |
| 1–4 часа | 1 | 0 | −1.05 R |
| **больше 4 часов** | **6** | **0** | **−6.35 R** |

Срок заявки ИИ — 12 часов. AAVE налилась через 18 часов после разбора, UNI
через 9.7, DOT через 12. План построен на часовой структуре; через полсуток
от неё ничего не осталось, а лимит всё стоит.

### Причина 4 — тесные стопы и проскальзывание

| дистанция стопа | сделок | итог | издержки от риска |
|---|---|---|---|
| 1.5–2.5% | 13 | −9.50 R | 4.07% |
| больше 2.5% | 4 | −0.04 R | 2.10% |

Отдельно: **7 стопов из 15 пробиты глубже −1.15R**. ARB (−2.22R) и NEAR
(−1.71R) налились и вылетели внутри ОДНОЙ свечи.

### Отклонённые планы: почему модель к ним приходила

Главные ворота — «обоснование не о том плане», 32 случая, 20% всех
отказов. Это не геометрия, это модель противоречит САМА СЕБЕ: XLM — в поле
стопа L9, в `analysis.plan` L10, в `stop_why` снова L9; ADA — в поле L8, в
`stop_why` «Стоп за L9 (0.2435)». Уровень надо назвать в пяти местах —
поле, `analysis.plan`, `stop_why`, `tp_why`, `risk`, — и на длинном
структурированном ответе модель сбивается.

**УБИРАТЬ ЭТИ ВОРОТА НЕЛЬЗЯ.** Прогон отклонённых планов по факту: именно
эта группа дала бы −17 R. Путаница в ответе — надёжный признак плохого
плана, а не описка.

### Что предлагается добавить (по силе доказательств)

1. **Ворота встречной торговли** — запретить план против ДНЕВНОГО тренда
   либо требовать от него большего (выше R:R, обязательное подтверждение).
   Доказательство: 0 из 8, −8.4 R.
2. **Срок заявки ИИ 12 ч → 4 ч.** Доказательство: после 4 часов ожидания
   0 из 6, −6.35 R.
3. **Сократить число мест, где надо назвать уровень** — меньше прозы,
   меньше шансов сбиться, плюс освободится бюджет ответа.

### ОГОВОРКА, БЕЗ КОТОРОЙ ЧИСЛА ВРУТ

17 сделок и ОДИН режим рынка — растущий. «Не шорти бычий рынок» здесь
доказано на одном бычьем рынке; правило конвенциональное, но выборка мала.
Правило про срок заявки опирается всего на 6 наблюдений. Перед выкаткой
любого из трёх — перепроверить на большем ряду или выкатывать с готовностью
откатить.

- **Статус:** измерено 24.09.2026, решение после сводки 25.09.

## 36. В каком формате вообще держать эту модель (идеи + замер выбора цели)

Разговор 24.09.2026 о том, где нынешняя модель полезнее всего. Записано
как список возможностей, решение не принято.

### Что измерено про саму модель

**Читает верно, решает плохо.** Её описания точны: по шортам она сама
писала «старший порядок вверх», «4ч вниз, день вверх» — и это была правда
каждый раз. Но:

| | |
|---|---|
| исполнение ограничений словами | 0 из 4 доказанных случаев (стоп XRP, `now`, переделка, подсказка по стопам) |
| переделка плана по замечанию | 0 из 12, иногда побайтово тот же ответ |
| самосогласованность ответа | 32 отказа «обоснование не о том плане» (20%) |
| её оценка вероятности | заявляет 0.65–0.70, по факту 0.19 |
| направление | 2 удачных из 17; против дневного тренда 0 из 8 |

### Четыре формата

1. **Как сейчас — генератор плана целиком.** 22 минуты на разбор, код
   полицейски проверяет каждое поле, главные ворота ловят её
   самопротиворечия. −9.53 R на 17 сделках.
2. **Аналитик: она называет уровни — план собирает код.** Уходят
   самопротиворечия, кривая геометрия, выбор условия, бессмысленное `p`.
   Ответ короче — разборов в сутки примерно вдвое больше.
3. **Второе мнение по чужим сетапам.** SMC и FIBO находят механически,
   модель отвечает на один вопрос абзацем. Дёшево, чисто замеряется.
   Оговорка: похожее пробовали (п. 19), там провалился механический
   искатель — но SMC и FIBO работают исправно, валидировать есть что.
4. **Оракул режима раз в несколько часов.** Отклонено: макро-взгляд у нас
   непредсказателен (п. 24), а одна ошибка размножится на все пять
   стратегий.

### ВОЗРАЖЕНИЕ ВЛАДЕЛЬЦА И ЗАМЕР, КОТОРЫЙ ЕГО ПОДТВЕРДИЛ

Возражение: модель видит скопления ликвидности и ставит тейк туда; чистый
код так не сделает — значит выбор цели её сильная сторона.

Проверка: у каждого принятого плана взяли вход и стоп модели, а цель
подставляли четырьмя способами по ТОЙ ЖЕ таблице уровней, и прогнали
одинаково по часовым свечам (лимит 12 ч, стоп внутри свечи раньше цели):

| цель | планов | дошло | стоп | не вошли | ИТОГО R | медиана дистанции |
|---|---|---|---|---|---|---|
| **модель** | 41 | 6 | 17 | 18 | **+8.88 R** | 3.95 R |
| ближний уровень | 42 | 17 | 7 | 18 | −0.90 R | 0.41 R |
| ближний край ликвидаций | 38 | 7 | 14 | 17 | **+5.64 R** | 3.53 R |
| ближний пул/EQH-EQL | 32 | 10 | 7 | 15 | −3.42 R | 0.53 R |

**Возражение верное.** Выбор цели моделью бьёт все механические правила.
Ближний уровень достигается втрое чаще (17 против 6), но стоит 0.41 R —
выигрыши крошечные, проигрыши полные, итог отрицательный.

**И видно, откуда берётся её преимущество:** правило «ближний край
ликвидаций» даёт +5.64 R, то есть 64% результата модели. Карту считает
КОД, выбирает из неё МОДЕЛЬ, и вместе они лучше, чем каждый по себе.
Надбавка модели над лучшим механическим правилом — +3.24 R на 38–41
плане, около 0.08 R за план.

Оговорка: симулятор оптимистичен (на принятых планах даёт +3.6 R против
−13.0 R в жизни) и не воспроизводит условие входа. Но он ОДИНАКОВ для всех
четырёх вариантов, поэтому сравнение между ними осмысленно.

### ПОЭТОМУ ДЕЛИТЬ НАДО НЕ «МОДЕЛЬ ПРОТИВ КОДА», А ПО ВИДУ РЕШЕНИЯ

| решение | кому отдать | доказательство |
|---|---|---|
| какой уровень — магнит (ЦЕЛЬ) | **модели** | +8.88 R против −0.90 у механики |
| сторона сделки | **коду** (по дневному тренду) | 0 из 8 против тренда, −8.4 R |
| где стоп | **коду** (за инвалидацией, с проверками) | 10 планов из 14 с кривой геометрией; сегодня SOL дважды выбрал запрещённый уровень |
| как далеко цель | **коду** | цели стояли в 2–9.7 R при ходе рынка 1.2–2.7 R |
| из какой зоны вход | модель выбирает, код проверяет сторону слома | — |

То есть формат 2 остаётся верным, но формулировка «код решает» была
неточной: код считает кандидатов и арифметику, модель выбирает среди них
смысл. Забирать у неё выбор цели нельзя — это измеримо её сильнейшее место.

- **Статус:** список идей, решение после сводки 25.09. Скрипт замера —
  `tools/target_pick.py`.

## 37. «Отдать решение модели целиком»: опыт уже поставлен, и два вората вредны

Вопрос владельца 24.09.2026: если скормить модели все данные и отдать ей
решение без ворот кода — насколько хороши будут сетапы?

**Отвечать не нужно предположением: опыт уже поставлен.** Каждый
ОТКЛОНЁННЫЙ план — это запись того, что модель сделала бы, если её не
остановить. Прогоняем обе группы одной симуляцией и складываем.

| | планов | итог | дожили до сделки | из них к цели |
|---|---|---|---|---|
| код ПРОПУСТИЛ | 41 | **+8.88 R** | 23 | 6 |
| код ОТКЛОНИЛ | 84 | **−29.24 R** | 46 | 4 |
| **всё без ворот** | **125** | **−20.36 R** | 69 | 10 |

Свобода стоит примерно **29 R**. «Модель решает всё» — измеримо хуже, чем
то, что есть сейчас.

**ВАЖНО: это не про выдумки.** Модель ничего не сочиняет — уровни берутся
из таблицы, грамматика пускает только их номера. Её ошибки другие:
противоречит себе между полями (31 план), торгует против структуры,
которую сама же назвала, ставит стоп в зону, которую сама же отметила.
Данные она читает верно; подводит решение.

### НО ДВА ВОРОТА ОКАЗАЛИСЬ ВРЕДНЫМИ

| ворота | планов | итог отклонённых | дожили | к цели |
|---|---|---|---|---|
| обоснование не о том плане | 31 | −17.00 R | 17 | 0 |
| мало конфлюенса | 14 | −7.00 R | 7 | 0 |
| план против структуры | 7 | −5.00 R | 5 | 0 |
| вход на пуле стопов | 8 | −3.34 R | 8 | 1 |
| условие противоречит входу | 6 | −3.00 R | 3 | 0 |
| стоп внутри зоны входа | 5 | −2.00 R | 2 | 0 |
| **стоп внутри живой зоны** | 5 | **+1.81 R** | 2 | 1 |
| **стоп в скоплении стопов** | 8 | **+6.29 R** | 2 | **2** |

Последние два отклонили планы, которые принесли бы деньги. Это ровно те
ворота, что глушили разборы 23–24.09 (ночью 4 отказа из 13, сегодня SOL).

**ПОПРАВКА К МОЕМУ ЖЕ УТВЕРЖДЕНИЮ.** 24.09 я написал «ни одни ворота не
отклоняли прибыльный класс планов» — это было неверно. Тот прогон
восстанавливал планы сломанным разбором (уровень писался как «L6 (0.2395)»,
а искался голый «L6»), и до счёта доходило 17–79 планов вместо 84. С
исправленным разбором двое ворот поменяли знак.

**Оговорка:** у обоих ворот до сделки дожило по 2 плана. Две сделки — это
не доказательство, это подозрение. Но подозрение сильное, потому что
совпадает с тем, что эти же ворота дают больше всего отказов сейчас.

### Что с «скормить ВСЕ данные»

Проверено, чего у модели нет: 15-минутного кадра (п. 33 — не стоит),
тепловой карты CoinGlass (п. 28 — у нас своя, лучше для Bybit). Всё
остальное она уже видит: стакан, ленту, дельту агрессора, ОИ, фандинг,
профиль, VWAP, сессии, обе карты ликвидаций, макро, свой счёт по прошлым
планам.

**Единственная настоящая дыра — новостной фон.** Параметр `news` в
`llm_context.build` есть, но всегда None, и в разметке честно написано
«фона нет». Источника у нас нет. По правилу проекта его сначала надо
логировать, потом использовать.

- **Статус:** «свободу модели» отклонить (−29 R). Ослабление двух ворот —
  кандидат номер один после сводки 25.09, но сперва набрать больше
  наблюдений: сейчас по 2 сделки на каждые.
- **ПОПРАВКА 25.09.2026 (п. 48): «+1.81 R» и «+6.29 R» не подтвердились.**
  Этот прогон (`target_pick.run`) не знал условия входа и ставил стоп на
  сам уровень. С исполнением бота (условие, заявка 12 ч, отступ стопа,
  комиссии) «стоп в скоплении стопов» — 8 планов, до входа не дошёл ни
  один (0 R); «стоп внутри живой зоны» — 9 планов, вошли 2, −1.25 R.

## 38. Почему модель торговала против тренда — причина была в НАШЕМ промте

### Цепочка, одинаковая во всех восьми встречных шортах

Модель правильно видела конфликт и проговаривала его вслух:

- LINK: «4ч/день — тренд вверх, но 1ч — локальный слом вниз (CHOCH 12.453).
  Bias: down (локальный контроль шортов, но **старший порядок вверх**)»
- AAVE: «4ч и День — тренд вверх… Bias: down (**коррекция в рамках старшего
  ап-тренда**)»
- SHIB: «4ч и День — восходящий тренд, но 1ч — локальный нисходящий импульс.
  Bias на сутки — down»
- DOT: «4ч — восходящий тренд, 1ч — нисходящий слом структуры»

Дальше везде один довод: «магнит вниз — скопление стоп-лоссов лонгов».

### Причина — в промте, а не в модели

```
llm_prompt: direction  тренд 1ч/4ч/день … → bias: up, down или flat
            liquidity  магниты ПО ХОДУ bias — цели
            structure  здесь выбирается УРОВЕНЬ ВХОДА ПО ХОДУ bias
```

**Мы просили ОДИН `bias` из трёх таймфреймов и не давали правила
старшинства.** Модель брала самый свежий сигнал — часовой слом — и весь
план механически шёл за ним. Часовой откат внутри восходящего тренда
превращался в СТОРОНУ сделки. Прежнее правило «старший ТФ против bias 1ч →
только с условием, p не выше 0.55» трактовало конфликт как риск, который
надо уценить, а не как признак того, что bias выбран неверно.

### И ВТОРОЕ: промт противоречил сам себе

```
словарь:  «после выноса с возвратом рынок идёт ОТ пула прочь:
           вынос EQL → ВВЕРХ, вынос EQH → вниз»
порядок:  «магниты по ходу bias — цели»
```

Словарь говорит, что снятие минимумов ведёт ВВЕРХ; порядок разбора
разрешал считать те же минимумы целью шорта. Модель выбирала второе.

### Что исправлено 24.09.2026

- `bias` задаётся 4ч и днём — это СТОРОНА; час решает где и когда входить;
- правило выноса связано со стороной прямо в словаре;
- «план согласован по одному ТФ» уточнено: план ИСПОЛНЯЕТСЯ на часе,
  сторону задаёт старший;
- магниты против bias названы местами входа, а не целями;
- код: ворота `план против старшего тренда` (`plan_against_higher_trend`),
  срабатывают ДО проверок геометрии. Молчат, если структуры 4ч нет.
- промт переэкспортирован в `docs/Промт_ИИ_v2.txt`.

### СИМУЛЯТОР ПЛАНОВ НЕПРИГОДЕН ДЛЯ ОЦЕНКИ СТОРОНЫ — ВАЖНО

Бэктест новых ворот дал, что они остановили бы планы на **+19.41 R**, то
есть якобы вредны. Проверка симулятора против ЖИВЫХ сделок:

| пара | живой итог | симулятор |
|---|---|---|
| NEARUSDT LONG | **+3.07 R** | стоп через 1 ч |
| NEARUSDT LONG | **+3.33 R** | стоп через 1 ч |
| SUIUSDT SHORT | **−1.06 R** | цель через 8 ч |

Симулятор называет стопами обе крупные победы и победой — реальный убыток.
Причина: он наливает лимит СРАЗУ, а живые сделки ждали УСЛОВИЯ входа (NEAR
вошёл после отката, симулятор — до него).

**Что из-за этого пересматривается:**
- п. 37 («свобода стоит 29 R») — сравнение остаётся осмысленным, обе
  группы считались одинаково, но абсолютные числа завышены;
- п. 36 (выбор цели, +8.88 R) — ОСТАЁТСЯ в силе: там вход и стоп
  фиксированы, менялась только цель, ошибка входа одинакова у всех
  четырёх вариантов;
- бэктест ворот — НЕ ДОКАЗАТЕЛЬСТВО ни за, ни против.

**На чём тогда стоят ворота:** только на живых сделках — против тренда 4ч
0 из 6 (−6.32 R), против дневного 0 из 8 (около −8.4 R) — и на механизме
(п. 33: при несогласной 4ч цена возвращается в 73% случаев). Шесть-восемь
наблюдений в ОДНОМ растущем режиме. Это тонко, и если после 25.09 ворота
начнут глушить прибыльные планы — откатывать.

- **Статус:** выкачено 24.09.2026. Следующий симулятор обязан
  воспроизводить условие входа, иначе им нельзя судить ничего, кроме
  сравнений при фиксированном входе.

## 39. Глубокий разбор всего раздела ИИ 24.09.2026: метод и находки

Задача: не гадать, а сверить каждое число и утверждение в промте с тем,
что реально делает код — построчно, как уже дважды сегодня сработало
(срок заявки, чек-лист перед планом). Метод: пройти `llm_prompt.py`
целиком, для каждого числа/правила найти источник в коде и проверить, что
они совпадают.

### Найдено и исправлено (пять расхождений)

1. **«Лимит живёт до трёх суток»** — было верно до 23.09 (общий запасной
   `PENDING_ORDER_MAX_HOURS`=72ч), с 23.09 `expiry_hours('LLM')` читает
   `LLM_TRIGGER_TTL_H`=12ч (та же заявка живёт столько же, сколько план
   ждёт условия). Текст не поменяли три дня. Исправлено: «лимит на entry и
   условие входа живут {ttl} часов — не дольше плана, который их держит».
2. **Чек-лист «ПЕРЕД plan СВЕРЬ» — сразу три стухших числа в одном
   предложении:**
   - «достижима за 1–2 суток» — осталось от многодневного плана; с 23.09
     план часовой, цель ограничена ДНЕВНЫМ РАЗМАХОМ за ЧАСЫ (раздел 0
     `ai_strategy.md`). Совпадает с `target_out_of_reach`, но не с этой
     строкой.
   - «R:R ≥ 2.5» — `MIN_RR` снижен до 2.0 ещё 23.09 (`4a7450c`). Тремя
     строками выше в том же документе `{min_rr}` корректно подставляет 2 —
     чек-лист жил отдельной жизнью.
   - «старший ТФ не против — иначе только условие и p ≤ 0.55» — старое
     мягкое правило, час назад (см. п. 38) заменено на жёсткий запрет
     (`plan_against_higher_trend`). Чек-лист всё ещё предлагал модели
     лазейку, которой уже нет.
   Та же стухшая «1–2 суток» повторялась в описании поля `tp_why`.
   Одинаковая причина у всех пяти: узкий чек-лист и описание поля не
   трогали при трёх подряд переписываниях основных правил (23.09 дважды,
   24.09 один раз), хотя каждое из них меняло именно то, что в чек-листе
   сказано.
3. **`legal_stop_ids` не знало о воротах против старшего тренда** —
   собственная сегодняшняя нестыковка между двумя патчами одного дня.
   Таблица «ГОДЯТСЯ ПОД СТОП» при бычьей 4ч показывала «ШОРТ — L1, L2»,
   хотя `plan_against_higher_trend` отклонит такой план целиком независимо
   от стопа. Подтверждено прогоном до и после правки. Исправлено: сторона,
   забракованная трендом, стопов не получает вовсе — таблица снова
   говорит правду. Новый тест
   `test_a_side_the_trend_forbids_gets_no_stops_at_all`.
4. **Два числа в `definitions()` были литералами, не подстановкой** —
   «профиль по 200 свечам» и «зона стоимости 70%» набраны в f-строке
   напрямую, хотя рядом в том же файле есть `llm_market.PROFILE_BARS` (200)
   и `llm_market.VALUE_AREA` (0.70). Числа СЕЙЧАС совпадают случайно — та
   же уязвимость, что уже трижды сработала сегодня, просто ещё не
   выстрелила. Исправлено на подстановку из констант, как весь остальной
   `definitions()`.
5. Критику добавлено в список «код уже проверил» упоминание новых ворот
   против старшего тренда — чтобы не тратил место на то, что и так
   недостижимо (план, не совпадающий с трендом, до критика не доходит).

### Проверено и НЕ является багом (важные ложные тревоги)

- **«ответ не разобран» — 122 случая в `refused.csv`, ноль в
  `llm_calls.csv`.** Выглядело как молчаливая потеря записей. Проверка по
  времени: все 122 датированы 18.09 22:33 — 19.09 08:01, то есть ДО
  начала `llm_calls.csv` (19.09 09:06). Это тот самый исторический
  инцидент («сто девятнадцать одинаковых отказов подряд»), который и
  вызвал переход на «пишем каждый вызов» в `llm_journal.py`. Строк
  «Разбор ИИ не записан» (признак сбоя записи) в логе — ноль за всё время.
  Не баг, архивная запись до собственного же исправления.
- **`LLM_VERDICT_MAX_AGE_MIN`=50 мин** — вердикт старше предела
  выбрасывается как «вердикт устарел». В деградации памяти сегодня разбор
  с переделкой доходил до 68–72 минут — теоретически мог попасть под этот
  потолок. Проверено: таких отказов НОЛЬ раз за всё время
  (`refused.csv`). Риск реальный на будущее (переделка удваивает время
  разбора, а 50 мин выбирались для одиночного прохода), но пока не
  срабатывал. Присматривать после 25.09, если появится хоть один случай —
  поднимать потолок.

### Найдено, не тронуто — для решения

- **«ПУСТОЙ РЫНОК: активность суток ниже 0.6 от недели… p не выше 0.55»**
  — константа 0.6 не существует НИГДЕ в коде. Это чистая просьба текстом,
  без единой проверки в `llm_decide.py`. По сегодняшнему счёту (п. 38) это
  пятый подтверждённый класс правил, которые эта модель не исполняет
  словами (после: стоп XRP, `now` по умолчанию, переделка плана, подсказка
  по стопам). НЕ добавлено воротами сейчас — это новая логика, а не
  правка текста, и без измеренного эффекта (как было у `plan_against_
  higher_trend`, которую подтвердили 748 наблюдений). Кандидат на
  измерение и, вероятно, ворота — после 25.09.
- `LLM_MAX_HOLD_HOURS`=336 (14 суток) — генеральный потолок удержания
  позиции при часовом плане. Ничего не ломает (максимальная сделка держала
  56.5 ч), но рамка широкая для стратегии, которая сама себя описывает как
  «час, не дни». Не трогать без данных.

### Тесты и выкатка

1321 passed, 8 skipped. Промт переэкспортирован
(`docs/Промт_ИИ_v2.txt`), диф — только по трём числам из п. 1–2.

## 40. Одна пара — несколько стратегий: решение владельца 24.09.2026

- **Откуда вопрос.** Проверка охвата 24.09: у ИИ реально доступно 11 пар из
  21 — девять держали FIBO (COTI, AVAX, BTC, DOT, LINK) и SMC (SHIB, NEAR,
  ETH) плюс заявка FIBO на AAVE. BTCUSDT не видела ИИ 65 часов подряд не по
  недостатку повода, а по замку `PAPER_EXCLUSIVE_PAIRS`.
- **Флаг появился 19.09.2026 не случайно.** Комментарий в коде: «на бирже
  позиция по инструменту одна: две стратегии на одной паре в бою
  невозможны, вторая пропускает. Фантом раньше разрешал обеим — и его
  результаты были завышены: каждая стратегия торговала так, будто соседей
  нет». Включён по замечанию стороннего разбора.
- **Решение владельца 24.09.2026, явное:** не ограничивать стратегии друг
  другом. `PAPER_EXCLUSIVE_PAIRS` — теперь `0` по умолчанию (было `1`).
- **ЦЕНА РЕШЕНИЯ, ЗАПИСАНА, А НЕ СПРЯТАНА:** фантомные результаты снова
  завышены относительно боя, как до 19.09.2026, — каждая стратегия торгует
  так, будто других нет, а на реальном счёте одновременно осталась бы
  только первая открытая позиция по инструменту. Числа между стратегиями
  после 24.09.2026 сравнивать осторожнее, чем числа 19–24.09.
- **Что осталось без изменений:** одна пара — одна позиция ВНУТРИ каждой
  стратегии (`has_position_or_order`) — это не про лимит биржи, а про то,
  что стратегия не открывает сама себе вторую позицию по паре, которую уже
  держит. Прежнее поведение (эксклюзивность между стратегиями) доступно
  ключом `PAPER_EXCLUSIVE_PAIRS=1` — например, для месячного сравнения
  ИИ/SMC на честных условиях, если до него дойдёт (см. отложенное «месячное
  сравнение на отдельных депозитах»).
- **Тесты:** `test_paper_broker.py::TestParallelStrategies` переписан под
  новое умолчание, старое поведение проверяется явным ключом
  (`test_the_second_strategy_is_refused_on_a_taken_pair_when_isolated`),
  добавлен `test_a_strategy_still_refuses_its_own_second_position` на то,
  что осталось неизменным. 1322 passed.
- **Статус:** выкачено 24.09.2026. Эффект на охват ИИ смотреть вместе со
  сводкой после 25.09 — теперь ей доступны все 20 ликвидных пар (BICOUSDT
  по-прежнему отсеян фильтром объёма, п. 39 — это отдельная причина, не
  эксклюзивность).

## 41. Таблица скрывала сторону-специфичный слом структуры — найдено и исправлено

- **Первая версия этой записи (24.09, до третьего случая) предполагала:**
  модель сознательно выбирает зону входа с 4ч в обход часового слома,
  растянув вчерашнее правило «сторону задаёт старший ТФ» на выбор зоны
  входа тоже. Гипотеза была правдоподобной, но неверной — третий случай её
  опроверг и показал настоящую причину.
- **Третий отказ, SOLUSDT 24.09 17:03:** модель сама написала в разборе
  «Структура 1ч цела (цена выше L12)», и в её собственной таблице L12 =
  **113.95**. Код отказал по другому числу — **вход 118.31 выше слом
  структуры 1ч 114.85**. Не ошибка модели: число 114.85 в вопросе не
  показывалось вообще.
- **Причина — в общем слое, не в рассуждении модели.**
  `llm_market.structure_break()` считает слом ДВАЖДЫ на каждый ТФ: `price`
  (общий, «по текущему тренду») и отдельно `long`/`short` — тот, что
  реально проверяют ворота (`structure_break_price(market, tf, side)`, оба
  вызова с явным side). В таблицу уровней печатался только `price`.
  Проверено на всех трёх отказах 24.09:

  | пара | показано в таблице | проверено воротами | совпало |
  |---|---|---|---|
  | SOL | 113.95 | 114.85 | нет |
  | ADA | 0.236 | 0.2427 | нет |
  | SUI | *(строки не было)* | 0.9714 | нет |

  Три из трёх. Не философский спор таймфреймов — дыра в данных: нужное
  число либо подменено другим, либо отсутствует.
- **Почему участилось именно после правки п. 38.** До неё сторона плана
  чаще совпадала с направлением часа (откуда считается общий `price`),
  числа случайно сходились. После — модель чаще торгует ПРОТИВ локального
  часового направления, вслед за старшим ТФ (правильно), и это ровно
  случай, когда скрытое число расходится с показанным. Вчерашняя правка не
  сломалась — она обнажила старую, редко срабатывавшую дыру.
- **Исправлено 24.09.2026.** `llm_context` печатает `long`/`short` отдельными
  строками («слом структуры 1ч для шорта»), когда они существуют и
  отличаются от общего — таблица `plan_against_structure` подсказку
  (п. 32) теперь называет то же число, что проверит код. Дубликат не
  печатается, если сторона совпадает с общим уровнем. Тесты:
  `TestStructureBreakShowsBothSides` (4 теста, в т.ч. точно на числах SOL).
- **ПЕРВАЯ ПРОВЕРКА НА БОЮ, 24.09 18:03.** SOLUSDT — почти тот же план, что
  дал отказ до правки (вход 118.3 против 118.31 раньше). В таблице теперь
  верно стоит «L11 114.85 слом структуры 1ч для шорта» — правка работает,
  число видно. **План всё равно отклонён тем же числом**: вход 118.3 из
  зоны L3, стоп 119.72 (слом структуры 4ч) — зона входа и стоп оба с
  четырёхчасового масштаба, 1ч-число рядом проигнорировано.
- **Значит, дыр было, возможно, две.** Первая версия этой записи (модель
  сознательно предпочитает 4ч-зону) была отброшена, когда нашлась дыра с
  невидимым числом — но на этом примере модель ИГНОРИРУЕТ число, даже
  имея его перед глазами. Обе причины могли работать одновременно: одна
  объясняла несовпадение чисел В ТЕКСТЕ ОТКАЗА, другая — почему зона входа
  выбирается без оглядки на часовой уровень вообще. Один случай на новом
  коде — рано делать вывод.
- **ВТОРАЯ ПРОВЕРКА, 24.09 20:03.** XRPUSDT — тот же почерк, что на SOL.
  Таблица верно показала «L9 1.5109 слом структуры 1ч для шорта», модель
  сама написала в разборе: «Слом структуры 1ч для шорта у L9 (1.5109)
  **защищает идею снизу**» — прочитала и поняла правильно. Вход и стоп
  всё равно взяты с 4ч: entry L5 (1.567, «верх имбаланса 4ч»), стоп L2
  (1.5913, «верх имбаланса 4ч»), а в `stop_why` — «Стоп над L2 защищает
  слом структуры 4ч и **локальный экстремум**». Отказ: вход 1.567 выше
  1.5109.
- **Два случая подряд после правки видимости, один почерк.** Оба раза
  модель называет часовой уровень верно, признаёт его роль словами — и
  всё равно строит план от зоны с 4ч. Не путаница (число видно и
  прочитано), не игнорирование (роль произнесена вслух). Больше похоже на
  то, что в её процессе построения плана часовой слом — это ОДНА ИЗ
  ШЕСТИ вещей в разборе, а не ГРАНИЦА, за которую нельзя заходить при
  выборе входа. Промт требует второго, таблица подсказки устроена как
  информация, а не как ограничение.
- **Статус:** правка видимости исправлена, выкачена и дважды подтверждена
  работающей (число показывается верно и в обоих случаях названо
  дословно правильно). Вопрос обязательности — «границей ли служит число,
  когда оно видно» — открыт, 2 случая одного почерка. Копить дальше; на
  третьем таком случае есть о чём говорить прицельно: не видимость,
  а то, что шаг `structure` в промте не говорит прямо: зона с 4ч всё
  равно обязана лежать по живую сторону 1ч-слома для своей стороны, без
  исключений для масштаба зоны.

## 42. ИИ против SMC: тот же словарь, разная архитектура — что можно позаимствовать

Вопрос владельца 24.09.2026: сильно ли отличается анализ ИИ от анализа
механической стратегии SMC, раз обе называют себя smart money concept.

### Словарь общий, честно

Ордер-блоки, имбалансы (FVG), снятие ликвидности, слом структуры
(BOS/CHoCH), премия/дисконт от ноги, равные экстремумы — термины у ИИ
использованы верно, не для вида.

### Но архитектуры разные

**SMC** — жёстко взвешенная, откалиброванная бэктестом система
(`smc/params.py`):

```
CONFLUENCE_WEIGHTS = {
    'htf_bias_aligned': 1.0,   # ЖЁСТКИЙ ГЕЙТ, в счёте ВСЕГДА
    'premium_discount': 1.0,   # ЖЁСТКИЙ ГЕЙТ
    'ote_zone':          1.2,  # САМЫЙ ТЯЖЁЛЫЙ фактор
    'structure_break':   0.8,
    'fvg_present':        0.6,
    'liquidity_swept':    0.3,
}
MIN_CONFLUENCE_SCORE = 4.5    # порог подтверждён отдельным бэктестом
```

**ИИ** — свободное рассуждение текстом, без фиксированных весов, решение
следует из шести полей разбора, не из суммы баллов.

### Три конкретных расхождения

1. **Согласие со старшим трендом.** У SMC встроено с начала как жёсткий
   гейт (`htf_bias_aligned`, вес 1.0). У ИИ такого правила не было до
   **23–24.09.2026** — добавлено кодом только после того, как живой замер
   показал 0 выигрышей из 8 встречных сделок (п. 35, 38). SMC получила эту
   дисциплину бэктестом заранее, ИИ — через реальные убытки постфактум.
2. **OTE (зона Фибоначчи 0.618–0.79) — самый тяжёлый фактор SMC**, а в
   промте ИИ не упоминается ВООБЩЕ — проверено построчно по всему тексту
   `llm_prompt.py`, ни одного упоминания OTE, Фибоначчи, 0.618/0.786.
3. **Типы зон входа.** SMC по собственному бэктесту сузилась до ТОЛЬКО
   `ORDER_BLOCK` — брейкеры и фитильные зоны на часе «устойчиво убыточны»
   (просадка 22.3% → 41.2% с ними, комментарий в `smc/params.py`). ИИ
   такого ограничения не знает: SOLUSDT 24.09 18:03 строила план из
   «брейкер 1ч L3» — зоны, которую SMC у себя же запретила как убыточную.
4. **Сессии/киллзоны.** У SMC может быть жёстким запретом
   (`KILLZONE_AS_GATE`). У ИИ — мягкая просьба текстом («p не выше 0.55»),
   и, как нашёл аудит 24.09 (п. 39), код её вообще не проверяет.

### По сделкам сравнивать пока нечестно

```
LLM   17 сделок, −9.53 R, период 20–23.09
SMC    1 сделка, −1.08 R, период 19–20.09
```

SMC почти не торговала не от бездействия: 2028 отказов в журнале, из них
1512 — «пара занята» (старый замок между стратегиями, снят только 24.09,
п. 40) и 450 — предел издержек. Находит много, до сделки не доходит
почти ничего — причины другие, чем у ИИ.

### Кандидаты на заимствование у SMC (не сделано, на будущее)

- **OTE-зона как фактор входа** — добавить в шаг `structure` промта как
  ещё одно место, где выбирается уровень входа, с тем же весом, что и
  остальные факторы `cf`.
- **Запрет входа из брейкеров на часе** — SMC уже доказала это бэктестом;
  для ИИ потребует отдельного замера на своих данных, чужой бэктест
  переносить нельзя.
- **Жёсткий гейт по киллзоне вместо мягкой просьбы** — если после 25.09
  подтвердится (по аналогии с п. 39), что модель этот совет игнорирует.

### Статус

Справочное сравнение, ничего не меняет само по себе. Каждый кандидат —
отдельная задача со своим замером на НАШИХ данных, чужой бэктест SMC не
переносится на ИИ автоматически: разный источник решения (формула против
рассуждения), разная модель, разные данные калибровки.

## 43. Порог по п. 41 пройден многократно — правило про 4ч-зону дописано в промт

- **Замер ночи 24-25.09.2026:** 20 разборов с 24.09 19:00:58 (чистая точка
  отсчёта после правки переделки), **0 принято**. «План против структуры» —
  7 из 20 (35%), главный блокиратор. SUI один — 4 раза подряд по ОДНОМУ И
  ТОМУ ЖЕ числу (слом 1ч = 0.9714, не менялось всю ночь): каждый раз новый
  вход, каждый раз выше того же уровня.
- **Порог из п. 41 («на третьем случае — чинить прицельно») пройден
  многократно.** Это больше не наблюдение — доминирующая причина простоя.
- **Найдена вторая причина, не только повтор вчерашнего.** Правило «ВХОД —
  из зоны инициатора: ордер-блок, имбаланс (1ч ИЛИ 4ч)» разрешало
  4ч-зону явно, отдельной строкой от «ВХОД — по живую сторону слома
  структуры 1ч» (три строки ниже). Разрешение и граница не были связаны в
  одном предложении — синтезировать их в одно правило модель была не
  обязана.
- **Исправлено 25.09.2026, в двух местах промта:**
  - `ВХОД — из зоны инициатора`: добавлено «ЗОНА 4Ч ГРАНИЦУ 1ч НЕ
    ОТМЕНЯЕТ: вход из неё обязан лежать по живую сторону слома 1ч так же,
    как из часовой зоны» — граница неотделима от разрешения, в одном
    предложении;
  - шаг `structure` (где зона реально выбирается): добавлено «Зона 4ч —
    сверь со сломом 1ч: за мёртвой стороной не спасает, ищи ближе или
    skip».
- **Бюджет окна.** Первая версия правки не влезала — `test_the_markup_
  still_fits_the_context_window` падал на ~137 токенов сверх 95% от 12288.
  Не подняли порог теста — сократили текст правки втрое (цитата с «семь
  раз подряд» вынесена из промта в код и в этот журнал, в промте остаётся
  только правило). Тест проходит с запасом.
- **Тесты:** 1326 passed без изменений (правка текстовая, механики не
  трогает). Промт переэкспортирован, диф — ровно две вставки.
- **Статус:** выкачено 25.09.2026. Смотреть после накопления новой
  выборки: упадёт ли доля «план против структуры», и в частности —
  перестанет ли SUI повторять один и тот же отказ.

## 44. Сторож памяти: запас 240с был скопирован не по адресу

- **Откуда.** 24.09 20:33 сторож (`llm_guard.sh`) убил разбор AAVEUSDT,
  начатый в 20:31:51 — за 104 секунды до перезапуска, «моложе GRACE=240с»
  посчитал его безопасным потерять. Тот же класс ошибки, что чинили вчера
  для переделки (п. «переделка получает свою строку»), но по другой
  причине: не слепота к событию, а сам запас.
- **Запас 240с был скопирован из `restart_when_idle.sh` без пересмотра.**
  Там он нужен ради разового дедлайна в час на выкатку кода — готовность
  пожертвовать несколькими минутами свежего разбора, чтобы не ждать
  дольше часа. У `llm_guard.sh` дедлайна нет: не дождался — сторож придёт
  через полчаса заново, это ничего не стоит. Единственный правильный запас
  для него — ноль: если пара отдана и вердикт ещё не собран, ждать, сколько
  бы разбор ни шёл.
- **Исправлено 25.09.2026:** `GRACE=0` в `ops/llm_guard.sh`. Причина
  записана в самом файле с точным временем инцидента.
- **Статус:** выкачено 25.09.2026. Проверить по `llm_guard.log`, что
  сторож больше не убивает разбор моложе нескольких минут.

## 45. Рост памяти модели — кэш подсказок в ОЗУ; сторож рубил переделки

- **Откуда.** Проверка разборов 25.09 утром: в 06:03 сторож перезапустил
  модель посреди переделки ADAUSDT (отдана в 06:02:21). Модель 90 с не
  могла остановиться — считала — и получила SIGKILL.
- **Ошибка сторожа — правка п. «переделка получает свою строку» (24.09) не
  работала ни разу.** Строка «отдал модели на переделку» пишется в ту же
  секунду, что вердикт первого прохода («модель: N вход»), а проверка
  требовала строго «позже». Равные метки — «разбор окончен» — перезапуск.
  С 24.09 19:00 это **6 из 11 переделок**, каждая совпадает с перезапуском
  модели до минуты: SUI 21:27→21:32, SUI 22:53→23:00, SOL 00:42→00:46,
  SUI 02:02→02:07, SUI 03:22→03:30, ADA 06:02→06:03. Вдобавок cron зовёт
  сторожа раз в 30 минут, а заход ждёт до часа — в 06:03 модель
  перезапускали два захода сразу.
- **Причина роста памяти (п. 31, «что ещё не знаем»).** llama-server по
  умолчанию держит кэш подсказок в ОЗУ на 8192 МиБ (`--cache-ram`). Когда
  новый запрос отбрасывает больше половины слота (`f_keep < 0.5` в
  `server-context.cpp`) — а это прогрев префикса после каждого разбора,
  4898 из ~11 тыс. токенов, — сервер откладывает в кэш всё состояние
  прошлого разбора. У каждой пары своя разметка, эти копии не нужны
  никогда. Замер 25.09: 0.74 ГБ после перезапуска 03:32 → 2.74 (05:00) →
  3.75 (05:30) → 4.43 (06:00), то есть **1.4–1.7 ГБ в час**; потолок
  0.74 + 8 = 8.8 ГБ — ровно замер 24.09 через 21 час. «0.4 ГБ в час» из
  п. 31 — среднее за сутки, не скорость.
- **Во что обходилось:** 8 перезапусков модели за 11 часов ночи (порог
  п. 31 «чаще раза в три часа — искать причину» пройден), и каждый стоит
  следующему разбору ~7.5 минут пересчёта префикса (06:15 — 450 с).
- **Почему префикс от этого кэша не зависит.** Быстрый прогрев («4
  токена», 2.6 с в 05:38) даёт контрольная точка самого слота
  (`slot.prompt.checkpoints`, создаётся за 4 токена до конца запроса),
  откат к ней идёт внутри слота. Запись в памяти ассистента приписывала
  это кэшу в ОЗУ — неверно.
- **Исправлено 25.09.2026 (b4cad2e):** `--cache-ram 0` в
  `kraken-llm.service`; туда же перенесён `--repeat-penalty 1.05
  --repeat-last-n 256`, стоявший с 23.09 только на сервере (копия из
  репозитория молча сняла бы его). Сторож: сравнение «не раньше» в обоих
  скриптах, у `llm_guard.sh` замок `flock`.
- **Проверить:** (1) анонимная память держится около 0.7–1.2 ГБ несколько
  разборов подряд (было +~1 ГБ на разбор); (2) прогрев на ВТОРОМ разборе
  после перезапуска по-прежнему ~5 с — если каждый раз ~450 с, кэш был
  нужен, флаг откатить; (3) новых «модель перезапущена» в `llm_guard.log`
  нет.
- **Оставлено сознательно:** условие «вердикт забран циклом» в
  `llm_guard.sh` скопировано из `restart_when_idle.sh` и для перезапуска
  МОДЕЛИ не нужно (вердикт живёт в памяти бота), а при разборах встык окна
  не оставляет (05:00–06:00 «разбор шёл весь час»). Важно, только если
  память снова начнёт расти.
- **Статус:** скрипты и unit выкачены 25.09.2026 06:42; модель
  перезапускается разово сразу после ближайшего окончательного вердикта.

## 46. Переделка: 0 принятых из 11, две не влезли в окно

- **Замер 24.09 19:00 — 25.09 06:37**, 12 переделок (12-я, XLM, ещё
  идёт). Оборваны сторожем — 6 (п. 45). Дошли до ответа — 5:

  | пара | первый отказ | итог переделки |
  |---|---|---|
  | XRP 19:40 | стоп внутри живой зоны | план против структуры |
  | AAVE 22:06 | план против структуры | ответ обрезан (10472 + 1812) |
  | SUI 23:35 | план против структуры | ответ обрезан (10417 + 1867) |
  | XRP 01:20 | план против структуры | план против структуры |
  | ADA 02:38 | стоп внутри живой зоны | стоп внутри живой зоны |

  **Принято — 0.** Из трёх, что уместились, две повторили ту же ошибку.
- **Окно.** Вопрос переделки — 10.3–10.5 тыс. токенов (первый проход
  ~9.4 тыс. + отвергнутый план + причина), на ответ из 12288 остаётся
  ~1.8 тыс.; мысль съедает 900, план не дописывается. Тест бюджета окна
  (`test_the_markup_still_fits_the_context_window`) проверяет только
  первый проход.
- **Варианты (не решено):** переделка без мысли (`<think>` закрыт в
  подсказке — модель уже думала на первом проходе) освобождает 900
  токенов; в вопрос — только отвергнутые поля и причина, не план целиком;
  тест бюджета и на вопрос переделки.
- **Статус:** 25.09.2026 переделка переведена на ответ без мысли (п. 47).
  Смотреть: сколько переделок доходит до ответа и сколько из них принято.

## 47. Ноль планов 43 часа: что изменено 25.09 и как проверять

- **Замер.** Последний принятый план — 23.09 09:01 (DOT). С 23.09 12:00 —
  64 разбора, **0 планов** (до того — 33%, п. 30). Отказы: план против
  структуры 17, мало конфлюенса 12, стоп внутри живой зоны 8, модель
  пропустила 8, модель недоступна 5, нет законного стопа 4, против
  старшего тренда 3, стоп в скоплении 2, стоп внутри зоны входа 2, ответ
  обрезан 2, обоснование не о том плане 1.
- **Когда началось.** Коммит `4a7450c` (23.09 08:07): стоп за сломом
  структуры, план по часовому ТФ — и зоны 4ч в воротах «стоп внутри живой
  зоны». Последний план — через час после него.
- **Что изменено (владелец 25.09: «сделай, чтобы всё работало»):**
  1. План, который код отвергнет по слому структуры, стороне старшего
     тренда, стопу в зоне входа или в имбалансе 1ч, стал **невыразим**:
     грамматика пускает только законные ветки «сторона + вход + стоп»
     (`llm_decide.branch_filter`). Ворота в `check` остались вторым рубежом.
  2. Законных веток нет — модель не спрашивается: «нет законного плана»,
     как «нет законного стопа». Полчаса модели уходят на другую пару.
  3. Разметка печатает «ГОДЯТСЯ ПОД ВХОД» — диапазон уровней по живую
     сторону слома, рядом с «ГОДЯТСЯ ПОД СТОП».
  4. «Стоп внутри живой зоны» — только имбалансы 1ч; зона 4ч — пометка в
     `obstacles`. Основание: стоп часового плана стоит за часовым сломом
     (правило одного таймфрейма), 6 из 8 таких отказов — по зонам 4ч шириной
     до 5.5% цены. Довод п. 37 «+1.81 R» не подтвердился (п. 48): из
     отказов по зонам 4ч до сделки дошёл один (ZEC, открыт, −0.24 R).
  5. «Стоп в скоплении стопов» — пометка в `obstacles`, не отказ. Довод
     п. 37 «+6.29 R» не подтвердился (п. 48): ни один из 8 отклонённых
     планов до входа не дошёл бы — ворота ничего не сберегли и не стоили.
  6. Переделка — без мысли (п. 46).
  7. Эксплуатация: needrestart больше не перезапускает `kraken-*` (06:43
     автообновление Ubuntu перезапустило бота посреди переделки XLM);
     сторож берёт PID модели у systemd, а не `pgrep -f`.
- **Что НЕ ослаблено:** мало конфлюенса (−7 R у отклонённых), против
  старшего тренда (весь убыток стратегии — встречные сделки, п. 35),
  обоснование не о том плане (−17 R), вход на пуле, R:R, минимальный стоп,
  предел издержек, EV.
- **Проверить через 1–2 суток:** (1) доля принятых планов; (2) исходы
  принятых с пометкой «стоп у скопления» или «стоп в зоне 4ч» — отдельно
  от остальных: хуже — вернуть ворота; (3) сколько разборов кончилось «нет
  законного плана» — сэкономленные получасы; (4) переделки: сколько дошли
  до ответа и сколько приняты.

## 48. Что принесли бы планы, отклонённые кодом (замер 25.09.2026)

- **Вопрос владельца:** сколько отклонённых кодом планов отработало бы и
  сколько принесли бы прибыли или убытка.
- **Метод — `tools/rejected_outcomes.py`:** все 150 ответов «войти» с
  19.09 09:06 по 25.09 07:04 (у переделки — последний план «войти»),
  исполнение как у бота: «рынок обогнал план» по трём часам к вердикту,
  условие входа по закрытым часам до 12 ч, цель раньше условия — план
  снят, заявка 12 ч, стоп за уровнем с отступом, комиссии, выход по
  первой цели или стопу. 1R = $50.

  | | планов | вошли | цель | стоп | открыто | итог |
  |---|---|---|---|---|---|---|
  | приняты кодом | 42 | 9 | 4 | 5 | 0 | +6.91 R |
  | **отклонены кодом** | **108** | **26** | **2** | **21** | 3 | **−15.3 R ≈ −$765** |
  | из них с 23.09 12:00 | 40 | 6 | 0 | 3 | 3 | −3.0 R ≈ −$150 |

  По воротам (всё время): обоснование не о том плане −7.23 R (7 входов,
  0 целей), план против структуры −4.02 R (5 входов, 0 целей), условие
  против входа −2.06 R, стоп внутри живой зоны −1.25 R, стоп внутри зоны
  входа −1.03 R, вход на пуле −0.44 R, стоп в скоплении стопов 0 (8
  планов, ни одного входа), против старшего тренда 0, мало конфлюенса
  +0.89 R (5 входов, 1 цель).
- **Большинство планов до сделки не доходят вовсе:** из 108 отклонённых не
  вошли бы 82 — условие не наступило 39, рынок обогнал 22, цель без входа
  13, не налился 8. Все 150 планов — с условием, чаще всего sweep_reclaim.
- **Симулятор оптимистичен:** на принятых он даёт +6.91 R, в жизни 17
  сделок ИИ дали −$474.95 (2 цели, 15 стопов). Часть разницы — заявки до
  21.09 жили 72 ч и наливались поздно. Для отклонённых это значит, что в
  жизни было бы ещё хуже; знак вывода от этого не меняется.
- **Вывод:** ворота кода в сумме сберегли около 15 R (~$765). Ноль планов
  с 23.09 — не упущенная прибыль: отклонённое за эти двое суток дало бы
  −3.0 R, ни одной цели. Доводы п. 37 в пользу ослабления двух ворот
  (+1.81 R, +6.29 R) не подтвердились; сами ворота здесь ничего не решали
  — почти всё, что они отсекли, до входа не доходило. Уточнение — п. 49.

## 49. Где мы ограничиваем ИИ: идея против правил исполнения (замер 25.09)

- **Вопрос владельца:** сколько из отклонённых планов были бы успешны, не
  ограничиваем ли мы модель.
- **Метод — `tools/idea_vs_rules.py`:** 158 планов «войти» (116 отклонённых,
  42 принятых). «Идея» — что цена задела раньше после вердикта, цель или
  стоп, без всякого входа. Исполнение — четыре способа: как у бота, лимит
  без условия 12 ч и 48 ч, вход сразу по рынку.

  | | идея права | бот | лимит 12 ч | лимит 48 ч | по рынку |
  |---|---|---|---|---|---|
  | отклонённые (116) | 39% (33 из 85) | −15.2 R | −16.2 R | −19.7 R | −17.1 R |
  | принятые (42) | 48% (19 из 40) | +6.9 R | +14.7 R | +12.3 R | +13.5 R |

- **Ворота разделяют хорошо:** отклонённое в минусе при ЛЮБОМ исполнении,
  принятое — в плюсе при любом. Сильного ограничения воротами в целом нет.
- **По воротам (бот / лимит 12 ч / лимит 48 ч / по рынку):** обоснование
  не о том плане −7 / −17 / −24 / −19 R, против структуры −4 / −2 / −5 /
  −5 R — защищают. **Мало конфлюенса (19 планов): +0.9 / +0.8 / +5.8 /
  +3.5 R, идея права в 9 из 16** — единственные ворота в плюсе при всех
  способах; порог 4 фактора из 5, отклонённые набирали 3. Стоп внутри
  живой зоны (10): −1.4 / +4.1 / +2.8 / +4.0 R; стоп в скоплении стопов (8):
  0 / +4.3 / +4.3 / +2.7 R — обе ослаблены 25.09 (п. 47), теперь это
  подтверждено и без условия входа.
- **Самое сильное ограничение — условие входа, которое ставит сама
  модель.** Все 158 планов — с условием (sweep_reclaim чаще всего). Бот
  вошёл бы в 35 из 158, лимит без условия — в 90. У принятых: +6.9 R с
  условием против +14.7 R без. Но 17 настоящих сделок по принятым дали
  −9.5 R — симулятор завышает на ~16 R, поэтому снимать условие по одной
  симуляции нельзя.
- **Шорты хуже лонгов:** идея права в 38% против 43%, по рынку −8.7 R
  против +5.1 R (неделя растущего рынка — оговорка п. 20).
- **Статус:** порог конфлюенса снижен до 3 (п. 50). Опыт с условием входа —
  отдельно и только с живой проверкой.

## 50. Какие свойства плана связаны с исходом (замер 25.09.2026)

- **Метод — `tools/plan_factors.py`:** те же 158 планов «войти», каждый
  разложен по признакам; исход — «идея» (цель раньше стопа) и R при
  исполнении ботом, лимитом без условия 12 ч и по рынку. Выборки малые —
  в дело идёт только то, что крупно и согласно по всем меркам.
- **Сверка симулятора с жизнью — `tools/sim_vs_real.py`.** Из 17 настоящих
  сделок 9 первых (19–21.09, восемь убыточных шортов) — ответы старого
  формата: план не разбирается, и ВСЕ замеры (п. 37, 48, 49 и этот) их не
  видят. Отсюда «симулятор +6.9 R против жизни −9.5 R». По остальным восьми
  симулятор совпал с жизнью в 7 случаях из 9 планов. Настоящие сделки с
  21.09 07:30: 2 цели, 6 стопов, итог +0.01 R — около нуля, не минус.
- **ПЕРВАЯ ЦЕЛЬ — САМОЕ СИЛЬНОЕ.** Цель на имбалансе, сломе, пивоте,
  экстремуме: идея верна в 74% (23 из 31), в плюсе при любом исполнении.
  Цель на пуле или крае ликвидаций: 32% (24 из 76), в минусе при любом. На
  всех дальностях одинаково — дело в виде уровня, а не в расстоянии. Модель
  брала пул и ликвидации первой целью в 92 планах из 158. **Исправлено
  25.09 в промте:** первая цель — структура, пул и ликвидации — второй.
- **ЧИСЛО ФАКТОРОВ ОБРАТНО КАЧЕСТВУ:** 3 фактора — 57%, 4 — 53%, 5 — 34%
  (92 плана с пятью — худшая группа). **Исправлено 25.09:** порог 4 → 3.
- **Фактор der (ОИ, фандинг, лонг/шорт «подтверждают»):** с ним идея верна
  в 34% (99 планов), без него — 69% (26); при равных четырёх факторах — 36%
  против 90%. Похоже на «подтверждение» = переполненная сторона. Одна
  неделя, один режим — **не трогаем, копим**; проверить через месяц.
- **Тренд 4ч:** «против 4ч» 64% против 36% «по тренду» — но весь выигрыш
  дали лонги против медвежьей 4ч (10 из 11) в растущую неделю: откуп в
  боковике, а не правило. Противоречит п. 35 (живые сделки против тренда
  проиграли) — там другое определение тренда. **Ворота не трогаем**;
  вопрос режима (тренд или боковик) — открыт.
- **Слабее, на малых выборках — копить:** вход «на пробое» (лонг выше
  цены, шорт ниже) — идея 21% (26 планов); условие close_below без объёма —
  9% (1 из 11); цель дальше 1.5 дневного размаха — 30%; стоп за
  ордер-блоком — 15% (3 из 20).
- **Проверить через 1–2 недели тем же инструментом:** (1) доля первых
  целей на пуле/ликвидациях упала; (2) идея и R у планов с тремя
  факторами; (3) der — держится ли разрыв на новом режиме рынка.

## 51. Правки 25.09 на живом рынке; следующий отказ — пул; переделка без отката

- **Живая проверка пп. 45–50 (08:04–09:02):** LINK — «нет законного
  стопа», AAVE — «нет законного плана» (модель не спрашивалась: раньше эта
  пара раз за разом тратила полчаса на «план против структуры»); ZEC —
  ответ по новой грамматике прошёл слом структуры, мысль закрылась сама на
  462 токенах. Разметка печатает «ГОДЯТСЯ ПОД ВХОД». Переделка без мысли:
  10905 из 12288 — влезла, 850 с вместо ~1350. Прогрев после разбора —
  4 токена за 1.5 с без кэша в ОЗУ; память модели 1.1–1.4 ГБ. Прогрев ZEC
  в 08:29 — полный пересчёт 465 с: сменился сам промт (цель, «3 из 5»), это
  однократно.
- **Следующий отказ — «вход на пуле стопов».** ZEC дважды (и первый
  ответ, и переделка): лонг от L8 «слом структуры 1ч для шорта / равные
  экстремумы EQH». Из 11 таких отказов в журнале 10 — пул ЧУЖОЙ стороны
  (лонг на стопах шортов), это отказ при любом условии. Ворота защищают:
  идея у отклонённых верна в 25% (п. 49). **Сделано 25.09:** такой вход
  невыразим в грамматике и не показывается в «ГОДЯТСЯ ПОД ВХОД»
  (`llm_decide.wrong_pool_entry`); пул своей стороны без sweep_reclaim по-
  прежнему ловит `check` (условие стоит после плана, грамматике не видно).
- **Переделка читала разметку заново.** Прогрев перед каждым вопросом
  откатывал слот к системному префиксу, и вопрос переделки дочитывался с
  нуля разметки: ZEC — 5283 токена, 580 с из 850. **Сделано 25.09:**
  вопрос, повторяющий прошлый на 80%+, не прогревается (`llm_server.
  _continues_last`) — сервер откатывается к своей контрольной точке за 516
  токенов до конца прошлого вопроса. Проверить в журнале модели: у
  переделки prompt eval ~1.5 тыс. токенов вместо ~5.3 тыс.
- **Модель простаивала после отказа без неё.** «Нет законного плана»
  готов за 0.4 с (журнал 0.07 + разметка 0.31), но вердикт забирал только
  следующий цикл: 25.09 09:16–09:31 AAVE, AVAX, LINK подряд — 15 минут
  модели впустую. **Сделано 25.09:** потоку отдаётся пачка до 3 пар со
  снимками; после отказа из `QUICK_GATES` он сразу берёт следующую, после
  ответа модели остаток возвращается в очередь. Цикл по-прежнему не ждёт
  модель; цена — снимки ещё двух пар (~7 с) в цикле, где модель свободна.
- **Почему столько «нет законного плана» (разбор 25.09 ~09:45, ветки
  лонг/шорт):** AAVE (0,1) без всяких ворот — стоп ≥ 1.5% по издержкам и
  R:R ≥ 2 не оставляют геометрии; AVAX (7,8) и XLM (20,7) — 4ч медвежий
  запрещает лонги, 1ч бычий (цена выше последнего LH, у XLM на 8%)
  запрещает шорты; LTC (8,9) — 4ч отсекает шорты, имбалансы 1ч — 7 лонгов
  из 8. Главное: **в боковике 4ч и 1ч смотрят в разные стороны, и правило
  «сторона — от 4ч, вход — по живую сторону слома 1ч» плана не допускает.**
  Это не поломка: когда час сломается в сторону 4ч, у пары появится повод
  (BOS/CHoCH в срочности) и законный план. Отвергнутые этим правилом планы
  проигрывали (п. 49). Открытый вопрос на будущее — план «с подтверждением»
  (вход от зоны 4ч после слома часа обратно), сейчас он невыразим.

## 52. После правок 25.09: главный отказ — стоп, о котором модель думает, запрещён

- **Замер 09:47–11:05.** Пачка: 16 пар за 20 минут, 14 отсеяны без модели
  («нет законного плана/стопа»), к модели — NEAR и ZEC. Оба — «обоснование
  не о том плане». Переделка ZEC дочитала 1630 токенов, 8976 — из кэша
  слота, 508 с (было ~850 и ~1350): откат без прогрева работает. Память
  модели 1.41 ГБ спустя 4 часа.
- **Механизм.** ZEC: модель хотела лонг от L9 (1570.69), стоп за L11
  (1547.81), цель L5 (1600) — R:R около 1. «← СТОП» в разметке стоял у
  одного L12; грамматика пустила единственный законный вариант (стоп L12,
  цель L1 1651), а текст остался о L11 и L5 — отказ. Переделка получила
  «обоснование говорит о L10, L11» без объяснения и повторила то же. Итог
  правильный (плана по правилам нет), но стоит 33 минуты модели.
- **Сделано 25.09:** в замечание переделки по этому отказу добавлен список
  законных стопов стороны: «годятся только L12 (помечены «← СТОП»), за
  другой уровень код стоп не пустит: построй обоснование под законный стоп
  или ответь skip». Ждём: переделка кончается согласованным планом или
  честным skip, а не повтором.
- **Попутно:** NEAR 10:29 в журнал не попал — выкатка 10:31 оборвала его
  переделку, а журнал пишется после неё; первый проход пропал вместе с
  ней. Выкатка больше переделку не рвёт (b95bceb).
- **Проверить:** доля «обоснование не о том плане» и исход переделок по
  нему (план / skip / повтор) за сутки.
- **Первый замер, 25.09 11:11–16:30 UTC:** переделок 5, из них закончено 4 —
  и все 4 повторили свой отказ: ZEC (обоснование не о том плане), SOL и SUI
  (цель недостижима), ADA (вход на пуле стопов). Подсказка со списком стопов
  была только у ZEC. Пока переделка не исправила ни одного плана; судить
  рано — нужно хотя бы 10 переделок по каждому виду отказа.

## 53. Модель почти не видела, чем кончились её прошлые разборы (исправлено 25.09)

- **Что было.** Блок «ПРОШЛЫЕ РАЗБОРЫ ЭТОЙ ПАРЫ (и что цена сделала после)»
  сличал разбор с наблюдением за ценой по совпадению минуты. Разбор пишется
  в потоке модели, наблюдение заводит цикл, забирая вердикт, — на 3 минуты
  позже в медиане, до 19. С 22.09 исход нашёлся у 12 разборов из 161:
  строка «→ с тех пор …» почти никогда не доходила до модели.
- **Второе.** В две строки блока шли и отказы кода ДО вопроса («нет
  законного плана/стопа», это почти половина журнала) и поломки — модель
  читала как свой разбор то, чего не писала, а её настоящий прошлый разбор
  вытеснялся.
- **Третье.** Строкам отказов без модели в llm_calls доставались токены и
  секунды прошлого вызова: 37 строк из 63 врали о размере вопроса.
- **Сделано 25.09:** исход — ближайшее наблюдение в пределах 30 минут после
  разбора; в блок идут только разборы модели; отказы без модели пишутся без
  статистики вызова; журнал для истории читается на 150 строк вместо 80.
  Строка исхода короче (проценты с одним знаком) — блок вырос примерно на
  60 токенов. Замер 25.09: живой вопрос до 9 414 токенов (23 497 знаков),
  2.46–2.63 знака на токен, окно 12 288 — запас есть. Проверка длины
  вопроса теперь собирает разметку С блоком прошлых разборов.
- **Проверить:** меняется ли поведение на паре после неудачного прошлого
  разбора — повторяет ли модель план, опровергнутый ценой (прошлый вход, цель
  не взята, стоп снят), и доля «вход против исхода прошлого разбора». Нужны
  сутки-двое разборов с новым блоком.

## 54. Воронка 23.09 12:00 – 25.09 18:30 UTC: почему ИИ почти не торговал

- **152 вызова:** 75 отказов кода ДО модели (54 «нет законного плана» —
  ни одно сочетание не проходит проверок, чаще всего 4ч и 1ч расходятся;
  21 «нет законного стопа»), 56 планов модели отклонены кодом, 10 раз модель
  сама ответила skip, 8 поломок (обрывы из-за старого сторожа памяти — до
  07:05 25.09, после — ни одной), 3 плана приняты — все 25.09, после правок.
- **До правок 25.09** отклонения — нарушения правил: «план против структуры»
  17 (SUI семь раз подряд предлагал один и тот же невозможный план — по
  20 минут модели каждый), «стоп внутри живой зоны» 8, «мало конфлюенса» 13.
  После грамматики законности (25.09) «против структуры» не было ни разу.
- **После правок** главный отказ — «обоснование не о том плане» (6 из 19):
  грамматика оставляет законный стоп, а текст модели — о другом уровне.
  Переделка не исправила ни одного (0 из 5). Ещё «цель недостижима» 3 (SOL,
  SUI дважды), «вход на пуле стопов» 2.
- **Принятые:** ZEC 12:22 → сделка 14:00–15:50, стоп −1.05R (была в +0.75R);
  DOT 14:05 → лимит 1.1641, ждёт (цена 1.18, прошла +1.46R без нас); SOL 14:28
  → условие sweep_reclaim 118 не наступило, а цена дошла до цели 122.25 —
  условие сняло выигрыш (ср. п. 15).
- **Пропускная способность:** медиана разбора 21.7 мин (до 41.9), около 2.7
  разбора в час; быстрые отказы кода модель не занимают.
- **Проверить через сутки:** долю «обоснование не о том плане» и что делать
  с переделкой по нему (отключить или принимать план с законным стопом, если
  идея та же); сколько принятых планов гибнет на условиях входа.

## 55. Переделка выключена — ГРАНИЦА ДАННЫХ (решение владельца 26.09.2026)

- **С 26.09.2026 06:17:03 UTC** (перезапуск бота с df5422e, строка в
  bot_data/deferred_restart.log) переделка выключена:
  config.LLM_REVISIONS=0 (вернуть — LLM_REVISIONS=1 в .env сервера).
- **Почему.** Из ~19 переделок 23–25.09 план не исправила ни одна (п. 46, 52,
  54): модель повторяла ту же ошибку, каждая попытка — ~8 минут модели, около
  часа в сутки.
- **Как это видно в данных.** llm_calls.csv, колонка `revision`:
  `сделана` — переделка была (ответ после замечания — в строке, первый — в
  `rejected_raw`); `выключена` — план отвергнут правилом, по которому раньше
  ушёл бы на переделку, второй попытки не было ПО НАСТРОЙКЕ; пусто — отказ не
  из переделываемых. То же поле — в llm_features.jsonl и в журнале сетапов ИИ
  (колонка «ИИ: переделка»). До 25.09 колонки не было: там переделку видно по
  непустому `revised_from`.
- **Для разбора и обучения.** Пары «до/после замечания» (для обучения
  предпочтениям) есть только ДО этой границы. После неё отказ с
  revision=`выключена` — это «до» без «после»: нельзя считать его
  окончательным суждением модели, ей просто не дали второй попытки. При
  сравнении доли принятых планов до и после границы учитывать, что раньше
  часть отказов могла стать планом после переделки (на деле — ни одна).

## 56. Вход уже за ценой: лимит, налитый хуже рынка (исправлено 26.09.2026)

- **Что было.** DOGE 26.09: LONG от L8 0.09869, стоп за L11 (0.097145),
  условие sweep_reclaim L11 0.09756. Условие наступило в 08:02 при цене
  0.0979 — лимит на покупку встал на 0.8% ВЫШЕ рынка. Биржа исполнила бы его
  сразу по рынку, брокер налил по 0.09869 (свеча открылась на 0.09761):
  −0.7R с первой свечи. XLM в 03:01 — зеркально: SHORT от 0.2177 при цене
  0.2200. Стоп от настоящей цены у обоих 0.77% при минимуме ИИ 1.5%, а
  издержки входа по рынку — около 10–14% риска при пределе 5%: такие сделки
  проверки не пропустили бы.
- **Раньше.** В закрытых сделках ИИ 8 наливов, где свеча открылась уже за
  лимитом: переплата 1.63R в сумме, 7 из 8 в минусе. Крупные — NEAR 20.09
  (0.93R; лонг после close_below, тогда ещё без запрета) и SUI 22.09
  (0.32R); мелкие (0.01–0.03R) могли быть честными наливами между
  постановкой и следующей свечой. Все восемь — после условия, не «now». У
  остальных стратегий — ни одного случая.
- **Почему план такой.** Модель пишет «ждём снятия L11 и возврата в зону
  входа» — то есть покупку на возврате ВВЕРХ к L8, по сути стоп-вход. А
  промт говорит, что условие взводит ЛИМИТ, который ждёт цену. После
  «выноса и возврата» цена стоит у уровня условия, а вход у DOGE выше
  уровня, у XLM ниже — значит, уже за ценой. DOGE пришёл через переделку
  (до 06:17): первый ответ код отверг как «лонг после закрытия ниже
  уровня», во втором модель сменила условие на sweep_reclaim, как
  подсказывал текст отказа, а вход оставила выше уровня.
- **Что сделано.** strategy_llm перед заявкой сверяет вход с ценой — и у
  взведённого плана при срабатывании условия, и у плана «now». Вход за
  ценой → план пересчитывается от цены (стоп и цели на месте) и проходит те
  же проверки: минимальный стоп плана, издержки входа ПО РЫНКУ (тейкер туда
  и обратно, 0.11%) при пределе 5%, R:R ≥ 2, ожидание
  (llm_decide.reprice_at_market). Не прошёл — отказ «вход уже за ценой»,
  план снят; наблюдение за вердиктом идёт как обычно. Прошёл — вход по
  рынку: в журнале сделки `entry_note`, а вход, названный моделью, — в
  `llm_entry_plan`. Брокер у ИИ исполняет лимит за рынком сразу и тейкером
  (strategy_profile.fills_through_market). У остальных стратегий
  переключатель свой и выключен — включать после замера на их движках.
- **Граница данных — 26.09.2026 08:53:32 UTC** (перезапуск на 2019625,
  строка в bot_data/deferred_restart.log). До неё вход ИИ за ценой записан
  по цене лимита — результат таких сделок в журнале занижен (перечень
  выше; открытые на границе DOGE и XLM — из них). После неё такой план —
  либо отказ «вход уже за ценой», либо вход по цене рынка (`entry_note` в
  журнале сделки). На границе взведено 4 плана; у одного из пяти, что
  висели утром (SHIB1000 LONG от 0.005833 при условии sweep_reclaim
  0.005738), вход стоял за уровнем условия — тот же рисунок, что у DOGE.
- **Проверить через неделю.** Сколько планов уходит в «вход уже за ценой»
  (панель, «Исходы планов ИИ») и что с ними стало по наблюдению — цель или
  стоп первыми. Если таких много — это вопрос к промту: модель ставит вход
  по ту сторону уровня условия. Правка промта — только с экспортом
  docs/Промт_ИИ_v2.txt.

## 57. Цели не по порядку (исправлено 26.09.2026); план против своего bias

Разбор всех 89 принятых планов (19–26.09) и 20 сделок ИИ.

- **Цели не по порядку — 11 планов из 89.** Модель перечисляет цели как
  хочет: ARB LONG — «цель L1, максимум недели» первой, ближний L2 второй
  ([0.23127, 0.22933]). Брокер берёт цели по порядку: после «первой»
  (дальней) «вторая» уже пройдена, остаток закрылся бы ниже рынка, а если
  цена дошла только до ближней — ничего не фиксировалось. R:R плана
  считался по первой цели, то есть по дальней, и порог 2.0 проходился ею.
  Исправлено: llm_decide.check упорядочивает цели от ближней к дальней
  вместе с номерами уровней (R:R и «цель недостижима» — по ближней);
  strategy_llm._reshape — у планов, взведённых раньше; брокер — у стоящих
  заявок при загрузке (заявка ARB от 26.09). Закрытых сделок это не
  коснулось: обе сделки ИИ с двумя целями (NEAR #41, #52) были по порядку.
- **Сторона против своего bias — 2 плана из 89:** SOL 21.09 LONG при bias
  down, XLM 26.09 SHORT при bias up (XLM сейчас открыта). Кодом не
  отсекается. Проверить по исходам, накопив 10+ таких планов: если хуже
  остальных — ворота «сторона против своего bias» (без правки промта).
- **Остальное в планах чисто:** стоп нигде не стоит ближе уровня
  инвалидации; вход за уровнем условия — 7 планов (п. 56, исправлено).
- **Сделки:** 18 из 20 — стоп. Все 9 шортов — 19–22.09, условием
  close_below, до правил старшего тренда и слома структуры (23.09). 6 стопов
  до стопа проходили +1R и больше — все до безубытка при +1R (22.09). По
  нынешним правилам (с 23.09) — 6 сделок: одна +3.33R, пять стопов.

## 58. Глубокий разбор ИИ: каркас доктрины убыточен сам по себе (замер 26.09.2026)

Данные: 464 разбора модели, 249 её планов, 91 принятый, 20 сделок
(19–26.09). Все принятые планы доведены до исхода по свечам Bybit
(`research/ai_live_plans.py`, сверка с журналом — 16 из 20 сделок по минуте
входа и исходу). Правила доктрины без модели прогнаны на пяти периодах
2022–2026 (`research/ai_doctrine.py`, результаты —
`research/results/ai_doctrine_run.txt`, `ai_doctrine_run_alt.txt`,
`ai_doctrine_ci.txt`, `ai_live_plans_run.txt`).

- **Почему столько стопов — три причины, по весу.**
  1. *Сторона против старшего тренда.* Все 34 шорт-плана шли против 4ч или
     дня (23 — против обоих; по `smc.structure` на момент плана). 16
     исполнимых — ноль в плюс, −11.55R. Модель сама писала «4ч и день
     вверх, но 1ч сломан вниз → bias down». Закрыто воротами 24.09 (п. 38).
  2. *Геометрия.* Стоп — медиана 2.0% = 0.31 среднего дневного размаха;
     первая цель — медиана 2.9R = 0.91 размаха, у 42% планов дальше него.
     Цель раньше стопа — у 12% сделок при p модели 0.65 (51 план из 91).
  3. *Сам каркас.* Доктрина, исполненная механически (сторона по 4ч и дню,
     вход в дисконте часовой ноги по живую сторону слома, стоп за HL/LH 1ч
     с отступом охоты, минимум 1.5% фильтром, ближняя ликвидность с R:R ≥ 2
     в пределах дневного размаха, заявка 12 ч, безубыток при +1R, 50/50), на
     той же неделе: 38 сделок, −0.23R на сделку, 53% стопов, стоп 2.05%,
     R:R 2.4. У модели: 34, −0.17R, 53%, стоп 2.0%, R:R 2.9. Модель
     торгует ровно так, как велит доктрина, и теряет так же.
- **Доктрина на истории — минус во всех пяти периодах:**

  | период | BTC | сделок | R на сделку, 95% интервал |
  |---|---|---|---|
  | bear 2022-01…2023-06 | −35% | 1453 | −0.137 [−0.204; −0.067] |
  | mid1 2023-07…2024-06 | +106% | 925 | −0.108 [−0.193; −0.020] |
  | mid2 2024-07…2025-04 | +50% | 997 | −0.064 [−0.149; +0.021] |
  | 12m 2025-05…2026-07 | −40% | 1243 | −0.078 [−0.148; −0.005] |
  | fresh 2025-11…2026-09 | −24% | 295 | −0.082 [−0.214; +0.056] |
  | все | | 4913 | **−0.098 [−0.135; −0.061], −484R** |

  Ни одно отступление не даёт плюса во всех периодах: сторона по 1ч или
  только по 4ч, без безубытка, одна цель 1R или 2R, вход по рынку, стоп
  за сломом 4ч, только лонги или только шорты, R:R ≥ 4 без безубытка (как
  SMC), вход после выноса HL со стопом за проколом (хуже доктрины везде,
  −0.14…−0.31R). Лучшие — стоп за сломом 4ч и цель 1R — около нуля.
- **Стопы не «выбивает шумом».** У 15 из 17 стопов ИИ цена через 12 ч ушла
  ещё дальше против сделки. У механической доктрины за сутки после стопа
  60% возвращаются к входу, до первой цели — 12–14%.
- **Модель не держит правила.** Код отклонил 158 из 249 её планов (63%),
  с 24.09 — 51 из 64 (80%): обоснование о других уровнях — 52, мало
  конфлюенса — 27, план против структуры — 20, вход и стоп на пулах — 21.
  Направление: `bias` на 24 ч верен в 41% разборов, «всегда лонг» на той же
  неделе — 63%; её «вниз» при растущих 4ч и дне — дальше +2.6% в среднем.
- **Пересмотр п. 19, 20 и 36.** «Навык входа» из п. 20 на 91 плане — это
  свойство лимита в дисконте, а не модели: те же 34 исполненных плана по
  рынку в момент плана дали бы −10.76R против −5.74R у бота, и у
  механической доктрины лимит лучше рынка во всех пяти периодах на
  0.03–0.04R. Но и с ним сделка в минусе. «Дальняя цель права» (п. 19) и
  «выбор цели — сильнейшее место модели» (п. 36) с исполнением бота не
  подтвердились: на тех же 34 сделках одна цель 1.5R даёт −2.97R, 2R —
  −3.33R, цели модели — −5.74R; на истории цель 1R лучше доктрины в 4
  периодах из 5. Замер п. 36 шёл без условия входа и отступа стопа.
- **Каркас SMC с живыми правилами** (`research/results/broker_rules_smc_*_close.txt`,
  «живые правила, заявка ждёт у цели»): 12m +93.7R (160 сделок), fresh
  +27.1R (138), mid1 +12.5R (157), mid2 +4.8R (121), bear +3.1R (248) —
  единственный каркас этой школы без минуса (12m и fresh частично
  перекрываются). Отличия от доктрины ИИ: строгий отбор зон (нетронутые,
  премия/дисконт, киллзоны, конфлюенс ≥ 4.5), R:R ≥ 4, выход 25/25/50, без
  безубытка, ~150 сделок в год на 10 пар против ~1000 у доктрины на 20.
- **Вывод.** Промт и ворота править бессмысленно: безошибочное исполнение
  этих правил теряет ~0.1R на сделку. Модели нужен другой каркас.
  Варианты — решение владельца: (а) ИИ — второе мнение к сетапам каркаса с
  замером (формат 3 из п. 36; своя копия правил отбора SMC, чтобы не
  связать стратегии, п. 42; модель отвечает «брать / не брать», вклад виден
  сравнением со «брать всё» на тех же сетапах);
  (б) ИИ не торгует, остаётся разбором для панели; (в) оставить как есть —
  не рекомендую: замер отвечает, статистика не изменит знак.
- **Статус:** наблюдаем до решения владельца.

## 59. Где ИИ может дать плюс: проверка гипотез (замер 26.09.2026)

Вопрос владельца: не наши ли ограничения и не промт ли заставляют модель
ошибаться, и где вообще ИИ может торговать в плюс. Данные: сетапы SMC, Фибо
(точная симуляция живой) и доктрины ИИ на пяти периодах 2022–2026 с
признаками на момент постановки, включая историю открытого интереса и
фандинга (`research/fetch_positioning.py`, `research/ai_setups.py`). Всё вне
выборки: учимся на трёх периодах, проверяем на четвёртом.

- **Наши ограничения — не корень** (`results/ai_doctrine_run_constraints.txt`).
  Минимальный стоп 1.5% ПОМОГАЕТ: при 0.8% доктрина хуже во всех пяти
  периодах. Ворота «цель дальше дневного размаха» ВРЕДЯТ: без них лучше в 4
  периодах из 5 (−0.038…−0.120R против −0.064…−0.137R), но всё равно минус.
  R:R ≥ 1.5 вместо 2 — чуть лучше в 3–4 периодах, тоже минус.
- **Промт ведёт модель вдогонку.** 75 из 91 принятого плана (82%) шли ПО
  ходу последних суток (медиана +2.65% в сторону сделки): 30 исполнимых —
  −0.288R на сделку; против хода — 4 исполнимых, +0.724R. Причина в тексте:
  «премия и дисконт — только от ПОСЛЕДНЕЙ НОГИ» (часовой) и «в тренде цена
  почти всегда выше POC — это норма, а не запрет на лонг»: откат часовой
  ноги внутри суточного рывка читается как дисконт. Но правило «не
  догонять» (вход только против хода суток) в бэктесте доктрины неустойчиво:
  bear +0.069, mid2 +0.045, а 12m −0.127, fresh −0.017, mid1 −0.017.
- **Направления в данных нет** (`results/ai_direction_study.txt`, 81 723
  точки). Гребневая регрессия на всех признаках разметки (ход 4ч–30д,
  диапазон, волатильность, объём, EMA, BTC, ОИ, фандинг): на 4 ч знак угадан
  в 50%, на сутки и трое — в трёх периодах из четырёх, ход меньше круга
  издержек. Между парами прогноз не работает; простой суточный импульс
  (верхние 3 минус нижние 3) +0.19% в сутки [+0.04; +0.35], во всех четырёх
  периодах, но за вычетом издержек +0.04%.
- **Сигналы позиционирования сами по себе** (`results/ai_signals_study.txt`,
  `ai_shock_study.txt`): крайний фандинг против толпы — знак пляшет;
  продолжение после объёмного выноса (> 3 ATR, объём > 3 медиан) — +0.24% за
  4 ч по закрытию свечи, но с входом на следующей 5-минутке и издержками —
  ноль или минус; на 12 ч плюс держится одним периодом.
- **Отбор сетапов — единственное, что работает** (`results/ai_filter_study.txt`,
  `ai_fibo_filter.txt`). Фандинг против толпы — самый устойчивый признак у
  всех трёх каркасов по отдельным заявкам. Фильтр Фибо вне выборки лучше
  «брать всё» в каждом периоде; портфель с живыми правилами брокера: все
  заявки −0.040R (5017 сделок), верхние 50% −0.003R, верхние 30% +0.017R
  (плюс в 3 периодах из 4), просадка вдвое меньше. У SMC фильтр вне выборки
  не помогает; правило «не входить туда, куда толпа платит» в портфеле SMC —
  +0.200R против +0.171R на сделку, лучше в 3 периодах из 5, сумма та же
  (`results/ai_smc_funding.txt`) — как правило не проходит.
- **Решение ФРС ±24 ч:** сделки SMC рядом −0.468R (90) против +0.024R,
  разность [−0.869; −0.030]; у Фибо и доктрины разницы нет. Один замер из
  многих — гипотеза для роли «события и новости», не правило.
- **Вывод.** Из чисел разметки ни модель, ни статистика прибыльного
  направления не извлекают; отбор даёт сотые доли R. Плюс в проекте есть
  только у каркаса SMC. Языковой модели остаётся то, чего в данных нет
  вовсе: текст (новости, объявления бирж, макро) — источник сначала
  логируется (правило проекта) — и разбор для человека.
- **Опыт с самой моделью** — 600 анонимных карточек сетапов Фибо (без
  монеты и даты), модель ставит вероятность цели раньше стопа; сравнение со
  статистикой на тех же пачках. Идёт на сервере, разборы ИИ на паузе.
- **Статус:** наблюдаем; итог опыта с моделью — отдельной строкой.

## 60. Почему голая SMC в плюсе, а ИИ в минусе (замер 26.09.2026)

Вопрос владельца: разделы ИИ и SMC построены на одних понятиях — откуда
разный знак? Ответ: словарь и данные у них общие, РЕШЕНИЯ — разные, и
правила, данные ИИ после аудита 22.09, отсекают у SMC-сетапов лучшее.

- **ИИ никогда не брал сетапы SMC.** Он строит план сам из таблицы уровней
  (любой: пивот, VWAP, POC, край имбаланса, ликвидаций). Комбинации «SMC +
  ИИ» в боте не было — в минусе собственные планы модели.
- **Проверка** (`research/ai_vs_smc.py`, `ai_vs_smc_reasons.py`,
  `results/ai_vs_smc_reasons.txt`): все сетапы боевого ядра SMC на 10 парах
  и пяти периодах (1533 исполненных, каждая заявка отдельно, +0.065R) и на
  каждом — правила ИИ по одному.
- **Правило стопа ИИ** (стоп за последним HL/LH часа + отступ охоты, вход по
  живую сторону слома, стоп не теснее 1.5%) делит сетапы SMC так:

  | группа | доля | итог у SMC | bear | mid1 | mid2 | 12m | fresh |
  |---|---|---|---|---|---|---|---|
  | вход за сломом часа — ИИ запрещено | 35% | **+0.230** | +0.087 | +0.606 | −0.163 | +0.318 | +0.291 |
  | стоп за сломом теснее 1.5% — ИИ запрещено | 41% | +0.049 | +0.006 | +0.050 | −0.394 | +0.346 | +0.014 |
  | законные для ИИ | 24% | **−0.145** | −0.212 | −0.081 | −0.150 | +0.017 | −0.277 |

  «Вход за сломом часа» у SMC — это вынос ликвидности под HL в нетронутый
  ордер-блок, классический вход smart money. Для SMC прокол HL — событие
  входа, для ИИ по нашему правилу — «тренд сломан, идеи нет». Идея входа от
  блока ломается за дальним краем блока (стоп SMC — край + 0.15%, медиана
  1.04%), а не за HL часа.
- **Цели ИИ** (ближняя ликвидность ≥ 2R, 50/50) вместо Фибо-расширений SMC
  (R:R ≥ 4, 25/25/50): −0.103R на сделку [−0.178; −0.031], хуже в 4 периодах
  из 5. **Сопровождение ИИ** (безубыток при +1R, снятие у цели, 12 ч):
  −0.148R [−0.306; −0.004], хуже в 4 из 5. Срок заявки 12 ч вместо 48 — без
  разницы.
- **Отбор.** У SMC — балл конфлюенса ≥ 4.5 (нетронутый блок, дисконт/премия,
  OTE, слом, FVG, вынос); у ИИ — суждение модели, которая держит правила в
  20–37% планов.
- **Оговорка.** Плюс SMC — бэктест (портфель с живыми правилами +141R за пять
  периодов); живьём с 21.09 у SMC одна сделка.
- **Следствие для правил проекта.** CLAUDE.md, раздел «Стоп»: «у ИИ с
  23.09.2026 это уровень СЛОМА СТРУКТУРЫ … край зоны — место входа, а не
  стопа» и «вход стоит по ЖИВУЮ сторону слома». Правило записано по 14
  планам аудита 22.09; на пяти периодах истории SMC-сетапов оно оставляет
  худшую четверть. Менять правило — решение владельца.
- **Статус:** решение владельца.

## 61. Модель как оценщик сетапов и режим «правила» (27.09.2026)

**Опыт A — модель оценивает сетапы сама.** 600 анонимных сетапов Фибо
2025–2026 (без монеты и даты, цены в процентах), пачки по 10, модель ставит
каждому вероятность цели раньше стопа; разборы ИИ на паузе, модель на
сервере, без мысли, температура 0 (scratchpad llm_exp/llm_rank.py).

| | верхние 5 из 10 | AUC |
|---|---|---|
| модель | −0.036R | 0.443 |
| всё подряд | +0.066R | — |
| статистика (гребневая, обучена на 2022–2025) | +0.189R | 0.550 |

Оценка модели ОБРАТНА исходу: сетапы, которым она даёт 0–30%, на деле
лучшие (42% в плюс, +0.244R). Модель ценит тесный стоп, близкую цель,
спокойный рынок и «неперегретую» цену; на деле выигрывают широкий стоп,
дальняя цель по R, высокая волатильность, ход по тренду. Добавка её оценки
к статистике вне выборки не помогает (AUC 0.534 → 0.520).

**Опыты B и C** — те же карточки с фактами о том, что работает (факты
только из 2022–2025), и модель как фильтр 400 сетапов SMC — идут; итог
отдельной строкой.

**Режим «правила» (LLM_MODE=rules, llm_rules, коммит fc0bdbc).** ИИ торгует
сетапы по своей копии правил отбора SMC (значения на 26.09.2026) на своём
пуле из 10 крупных монет, без входов ±24 ч от решения ФРС; модель входа не
выбирает. Ядро smc.signal принимает правила решений аргументом — правка SMC
ИИ не меняет и наоборот (тест изоляции «LLM-правила»). Бэктест ровно этого
режима (research/ai_rules_backtest.py, живые правила брокера):

| | сделок | итог | R на сделку |
|---|---|---|---|
| без календаря (= SMC сделка в сделку) | 824 | +141.2R | +0.171 [−0.016; +0.362] |
| **с окном ФРС ±24 ч** | 798 | **+155.7R** | **+0.195 [+0.007; +0.393]** |
| по периодам с окном ФРС | bear +8.0R, mid1 +15.1R, mid2 −9.3R, 12m +102.0R, fresh +40.0R | | |

Оговорки: пул SMC отобран по периоду 12m (вне отбора — bear, mid1, mid2,
fresh: +53.8R на 643 сделках, +0.084R); окно ФРС лучше в 4 периодах из 5, в
mid2 хуже на 14R; вживую SMC с 21.09 сделала одну сделку (направленный кэп
3/3 заявками). Режим выключен по умолчанию: включение — LLM_MODE=rules в
bot_data/.env и перезапуск через restart_when_idle.sh.

- **Статус:** режим готов, включение — решение владельца; B и C — ждём.

## 62. Направление по данным биржи: бустинг на истории; запись стакана (27.09.2026)

Главная задумка владельца — модель по данным биржи понимает, куда пойдёт
рынок. Языковая модель на исходах не учится, поэтому проверена верхняя
граница: обученный градиентный бустинг (scikit-learn; LightGBM 4.7 падает с
numpy 2.5) на 48 признаках, у которых есть история — ход цены на 7
горизонтах, волатильность, место в диапазонах, средние, объём, ОИ, фандинг,
премия, BTC, ширина рынка, ранги монет (research/ai_direction_ml.py, 165 174
точки решения, каждые 4 ч, 20 пар, 5 периодов). Торговля — знак прогноза в
сильнейших 20% (порог по обучению), издержки 0.21% на круг.

| горизонт | учится → торгует | знак | чистыми на сделку |
|---|---|---|---|
| 4 ч | итог «оставить один период» | 50–52% | −0.071% [−0.100; −0.042] |
| 24 ч | bear+mid1 → mid2 (рост) | 52.4% | +0.973% [+0.648; +1.289] |
| 24 ч | bear+mid1+mid2 → recent (падение) | 50.5% | −0.259% [−0.467; −0.050] |
| 24 ч | итог «оставить один период» | — | −0.076% [−0.180; +0.027] |

Модель выучивает режим своих учебных лет (главные признаки — место в
30-дневном диапазоне, общий фандинг рынка, 30-дневный ход), а не устойчивую
закономерность. Вывод: в данных с историей направления на 4–24 ч нет; то,
чего не находит обученная модель, языковая не найдёт.

Непроверенным остаётся стакан, лента и ликвидации (истории нет). Лента и
ликвидации пишутся с 18–19.09; стакан — с 27.09.2026 (positioning, источник
`book`: спред, перекос сторон на 0.1–2%, микроцена, крупнейшие плиты,
покрытие книги; глубина 500 уровней — на BTC это ~0.1%, на альтах 2–4%).

- **Статус:** ждём месяц записей стакана; затем ai_direction_ml.py с
  признаками микроструктуры на горизонтах 15 мин – 4 ч; принимать, только
  если вперёд по времени чистыми > 0 в обеих половинах.
