"""
Реакция SMC на режим рынка.

Определение режима переехало в market_regime.py — это состояние рынка, а не
свойство стратегии, и держать его внутри одного пакета значило бы заставить
остальные стратегии импортировать чужой.

Здесь остаётся только ответ на вопрос «что SMC делает, узнав режим»: в
трендовом режиме размер позиции умножается на REGIME_RISK_SCALE.

ЗАМЕР, НА КОТОРОМ ЭТО ОСНОВАНО (два независимых периода):

    режим            сделок   R/сделку   интервал среднего
    рост                 72     -0.090   [-0.566; +0.470]
    падение              77     +0.115   [-0.386; +0.664]
    боковик             349     +0.349   [+0.045; +0.672]   <- только здесь
    режим неизвестен    334     +0.319   [+0.040; +0.619]      не через ноль

В выраженном тренде цена не возвращается в зону интереса, и конструкция
работает вхолостую, неся при этом полный риск. Не запрет, а именно
уменьшение: полный запрет проверялся и оказался хуже — освободившиеся слоты
портфеля достаются худшим сетапам.

КОНТРОЛЬ, без которого утверждать было бы нечего. Любое сокращение риска
механически уменьшает просадку, поэтому мерилось против равномерного
сокращения на всех сделках. При совпавшей просадке адресный вариант даёт
заметно больше:

    бык:      в тренде 0.50 -> DD 18.0%, доход +408.9%
              равномерно 0.90 -> DD 18.1%, доход +362.8%
    медведь:  в тренде 0.25 -> DD 28.3%, доход +38.9%
              равномерно 0.75 -> DD 28.3%, доход +29.7%

Одинаковая просадка, разный доход. Работает адресность, а не сам факт
уменьшения размера.
"""

from market_regime import (RANGE, TREND_DOWN, TREND_UP,  # noqa: F401
                           UNKNOWN, classify, describe, efficiency_ratio)

from . import params


def risk_multiplier(regime):
    """
    Множитель риска SMC для режима. Единица — размер не меняется.

    Множитель НИКОГДА не превышает единицы: правило умеет только уменьшать
    ставку. Увеличение размера по сигналу режима не проверялось, а
    непроверенное увеличение риска — это не улучшение, а новый риск.
    """
    import market_regime
    return market_regime.risk_multiplier(regime, params.REGIME_RISK_SCALE)
